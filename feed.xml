<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://rosemary-crypto.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rosemary-crypto.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-03T06:49:07+00:00</updated><id>https://rosemary-crypto.github.io/feed.xml</id><title type="html">Rosemary Koikara - Cryptography, Blockchain, and AI Enthusiast</title><subtitle>Personal site for blog, projects, cv, etc. </subtitle><entry><title type="html">Adding Real Intelligence to Our Pipeline — YOLO on ONNX in C++, a GStreamer Plugin, and GstMeta</title><link href="https://rosemary-crypto.github.io/blog/2026/gstreamer-yolo-onnx-plugin-detections-metadata/" rel="alternate" type="text/html" title="Adding Real Intelligence to Our Pipeline — YOLO on ONNX in C++, a GStreamer Plugin, and GstMeta"/><published>2026-05-03T11:00:00+00:00</published><updated>2026-05-03T11:00:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2026/gstreamer-yolo-onnx-plugin-detections-metadata</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2026/gstreamer-yolo-onnx-plugin-detections-metadata/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="the-journey-ahead">The Journey Ahead</h2> <p>Remember how in our <a href="/blog/2025/understanding-gstreamer-buffers-and-plugin-development/">Lesson 2 article</a> we built a frame monitor and got comfortable with buffers, pads, and the chain function? That was <strong>Phase 1</strong> on our roadmap. And in <a href="/blog/2026/yolo-onnx-standalone-detector-cpp/">last week’s pre-Lesson detour</a> we built a tiny standalone YOLO ONNX detector in C++—the model bit, with no GStreamer in sight.</p> <p>Today we’re finally stepping into <strong>Phase 2</strong> by gluing the two together: a GStreamer plugin called <strong><code class="language-plaintext highlighter-rouge">yolonnx</code></strong> that runs the same kind of inference per frame, draws boxes on the video, and—the part I really want you to walk away with—<strong>attaches the detections to each buffer as <code class="language-plaintext highlighter-rouge">GstMeta</code></strong> so the rest of the pipeline can read them without re-running the network.</p> <p>This article is mostly about the <strong>GStreamer side</strong>—the plugin shape, the properties, and the meta API. If you want the model-loading and post-processing details, those live in the <a href="/blog/2026/yolo-onnx-standalone-detector-cpp/">previous article</a>.</p> <h3 id="where-this-fits-on-the-roadmap">Where This Fits on the Roadmap</h3> <ol> <li><strong>Phase 1 (done)</strong>: Frame monitor—our hello world for buffers and plugins.</li> <li><strong>Phase 2 (today)</strong>: A YOLO ONNX detector wrapped as a GStreamer element, with detections attached as <strong>GstMeta</strong>.</li> <li><strong>Phase 3</strong>: Object tracking — now we have something meaningful to track on each buffer.</li> <li><strong>Phase 4</strong>: Event detection and analytics.</li> <li><strong>Phase 5</strong>: Distribution and visualization.</li> </ol> <h2 id="step-1--stand-on-gstvideofilters-shoulders">Step 1 — Stand on <code class="language-plaintext highlighter-rouge">GstVideoFilter</code>’s Shoulders</h2> <p>In Lesson 2 we wrote our own chain function and pushed buffers around by hand. That was great for learning, but for a video element that produces video of the same shape it consumes, GStreamer has a much friendlier base class: <strong><code class="language-plaintext highlighter-rouge">GstVideoFilter</code></strong>. It handles caps negotiation, gives us a properly mapped <strong><code class="language-plaintext highlighter-rouge">GstVideoFrame</code></strong>, and lets us focus on the per-frame work.</p> <p>Our element subclasses it and only has to implement two interesting hooks:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">set_info</code></strong> — called once when the upstream caps are known, so we can stash the video format and size.</li> <li><strong><code class="language-plaintext highlighter-rouge">transform_frame_ip</code></strong> — called per frame, with the input frame already mapped read–write so we can draw on it in place.</li> </ul> <p>A couple of practical notes from getting this wrong the first time:</p> <ul> <li>We declare caps as <code class="language-plaintext highlighter-rouge">video/x-raw,format=(string){ BGR, RGB }</code>. Either layout is fine, and the helper that draws boxes flips channel order based on which one we got. <strong><code class="language-plaintext highlighter-rouge">videoconvert</code></strong> upstream takes care of the messier formats the decoder might produce.</li> <li>The frame plane stride is <strong>not</strong> always <code class="language-plaintext highlighter-rouge">width * 3</code>. We always go through <code class="language-plaintext highlighter-rouge">GST_VIDEO_FRAME_PLANE_STRIDE</code> when drawing.</li> <li>When something looks off, <code class="language-plaintext highlighter-rouge">GST_DEBUG</code> is still our best friend, same as in Phase 1.</li> </ul> <h2 id="step-2--properties-over-hardcoding">Step 2 — Properties Over Hardcoding</h2> <p>A plugin that hardcodes its model path and thresholds is a plugin that ages badly. The element exposes seven properties so you can tune behaviour from <code class="language-plaintext highlighter-rouge">gst-launch</code> without recompiling:</p> <table> <thead> <tr> <th>Property</th> <th>Default</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">model-path</code></td> <td><code class="language-plaintext highlighter-rouge">""</code></td> <td>Path to the ONNX file. Empty = synthetic detection mode.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">conf-threshold</code></td> <td><code class="language-plaintext highlighter-rouge">0.25</code></td> <td>Drop predictions below this score.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">iou-threshold</code></td> <td><code class="language-plaintext highlighter-rouge">0.45</code></td> <td>NMS overlap cutoff.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">draw-boxes</code></td> <td><code class="language-plaintext highlighter-rouge">true</code></td> <td>Paint rectangles directly on the video.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">attach-meta</code></td> <td><code class="language-plaintext highlighter-rouge">true</code></td> <td>Attach <code class="language-plaintext highlighter-rouge">GstDetectionsMeta</code> to each buffer.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">input-width</code></td> <td><code class="language-plaintext highlighter-rouge">640</code></td> <td>Network input width.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">input-height</code></td> <td><code class="language-plaintext highlighter-rouge">640</code></td> <td>Network input height.</td> </tr> </tbody> </table> <p>The <code class="language-plaintext highlighter-rouge">draw-boxes</code> and <code class="language-plaintext highlighter-rouge">attach-meta</code> split is on purpose. Set both to <code class="language-plaintext highlighter-rouge">true</code> while you’re debugging, then flip <code class="language-plaintext highlighter-rouge">draw-boxes=false</code> once a downstream element starts consuming meta on its own—you don’t want to re-render frames just to confirm something a sticky note already says.</p> <p>A useful side benefit: if you launch the element with no <code class="language-plaintext highlighter-rouge">model-path</code>, it falls back to a <strong>synthetic moving box</strong>. That sounds silly, but it’s the best teaching feature in the whole plugin—you can verify drawing, meta attach, and downstream readers without an ONNX file in sight.</p> <p>While you’re iterating, the pipeline looks like:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>clip.mp4 <span class="o">!</span> decodebin <span class="o">!</span> videoconvert <span class="o">!</span> <span class="se">\</span>
  video/x-raw,format<span class="o">=</span>BGR <span class="o">!</span> yolonnx model-path<span class="o">=</span>/path/to/yolo11n.onnx <span class="se">\</span>
  draw-boxes<span class="o">=</span><span class="nb">true </span>attach-meta<span class="o">=</span><span class="nb">true</span> <span class="o">!</span> videoconvert <span class="o">!</span> autovideosink
</code></pre></div></div> <h2 id="step-3--inference-without-the-drama">Step 3 — Inference, Without the Drama</h2> <p>Inside <code class="language-plaintext highlighter-rouge">transform_frame_ip</code> the work is exactly the recipe from the standalone post, just on a <code class="language-plaintext highlighter-rouge">GstVideoFrame</code> instead of a <code class="language-plaintext highlighter-rouge">cv::Mat</code>:</p> <ol> <li>Lazy-load the ONNX session on the first frame, so we honour whatever <code class="language-plaintext highlighter-rouge">model-path</code> was set at element configuration time.</li> <li>Resize the frame into the network’s expected size, normalize to <code class="language-plaintext highlighter-rouge">float32 / 255.0</code> in NCHW, RGB.</li> <li>Run the session.</li> <li>Decode the YOLOv8-style output (<code class="language-plaintext highlighter-rouge">[1, 84, 8400]</code> for an 80-class model), apply NMS, and return a <code class="language-plaintext highlighter-rouge">std::vector&lt;DetectionBox&gt;</code>.</li> <li>If <code class="language-plaintext highlighter-rouge">attach-meta</code> is set, append the boxes to a fresh <code class="language-plaintext highlighter-rouge">GstDetectionsMeta</code> on the buffer.</li> <li>If <code class="language-plaintext highlighter-rouge">draw-boxes</code> is set, paint green rectangles in place.</li> <li>Return <code class="language-plaintext highlighter-rouge">GST_FLOW_OK</code> and let the buffer continue down the pipeline.</li> </ol> <p>A handy debugging hook: at the bottom of inference, you can drop a single line like</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">g_print</span><span class="p">(</span><span class="s">"[yolonnx] frame=%u class=%d score=%.2f box=(%d,%d,%d,%d)</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
        <span class="n">self</span><span class="o">-&gt;</span><span class="n">frame_count</span><span class="p">,</span> <span class="n">det</span><span class="p">.</span><span class="n">class_id</span><span class="p">,</span> <span class="n">det</span><span class="p">.</span><span class="n">confidence</span><span class="p">,</span>
        <span class="n">det</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="n">det</span><span class="p">.</span><span class="n">y</span><span class="p">,</span> <span class="n">det</span><span class="p">.</span><span class="n">width</span><span class="p">,</span> <span class="n">det</span><span class="p">.</span><span class="n">height</span><span class="p">);</span>
</code></pre></div></div> <p>inside the loop and immediately see in the terminal which boxes the model is producing—class id, score, position, size. Comment it out once you trust the numbers; leave it commented in the source so future-you can re-enable it the next time something goes weird.</p> <h3 id="drawing-now-vs-fancy-overlays-later">Drawing Now vs. Fancy Overlays Later</h3> <p>For learning, I honestly recommend <strong>drawing directly on the video</strong> first. It’s the fastest feedback loop: either the box lands on the dog or it doesn’t. Later you can get fancy with <strong><code class="language-plaintext highlighter-rouge">cairooverlay</code></strong> (a Cairo-based overlay element) or <strong><code class="language-plaintext highlighter-rouge">gdkpixbufoverlay</code></strong>, or a separate branch that only emits meta. But a few nested <code class="language-plaintext highlighter-rouge">for</code> loops to paint green lines in BGR is enough to carry you into Phase 3.</p> <h2 id="step-4--gstmeta-sticky-notes-on-the-same-package">Step 4 — GstMeta: Sticky Notes on the Same Package</h2> <p>Drawing answers the question, <em>does the neural net agree with my eyes?</em> <strong>Metadata</strong> answers a different question: <em>what does the rest of the pipeline know without running inference again?</em></p> <p>Think of <strong><code class="language-plaintext highlighter-rouge">GstMeta</code></strong> as a <strong>sticky note</strong> you slap on the same package that already holds the pixels. Downstream elements—trackers, recorders, MQTT bridges—can peel off that note and read structured detections without paying the cost of another forward pass.</p> <p><a href="https://gstreamer.freedesktop.org/documentation/additional/design/meta.html">GStreamer’s meta system</a> lets you attach custom structs to a <strong><code class="language-plaintext highlighter-rouge">GstBuffer</code></strong> without copying the whole frame. The pattern looks like this:</p> <ol> <li><strong>Register</strong> your meta type once with <code class="language-plaintext highlighter-rouge">gst_meta_api_type_register</code> and <code class="language-plaintext highlighter-rouge">gst_meta_register</code>.</li> <li>Write little helpers like <code class="language-plaintext highlighter-rouge">gst_buffer_add_detections_meta</code> and <code class="language-plaintext highlighter-rouge">gst_buffer_get_detections_meta</code> so the rest of your code stays readable.</li> <li>After inference, <strong>fill</strong> a <code class="language-plaintext highlighter-rouge">GArray</code> of detection structs and attach it to the buffer.</li> <li>Downstream, <strong>get</strong> the meta and consume it until the buffer is recycled.</li> </ol> <p>Our two structs—the per-detection one and the meta itself—look like this:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="nc">_GstDetection</span> <span class="p">{</span>
  <span class="n">gfloat</span> <span class="n">x</span><span class="p">;</span>
  <span class="n">gfloat</span> <span class="n">y</span><span class="p">;</span>
  <span class="n">gfloat</span> <span class="n">width</span><span class="p">;</span>
  <span class="n">gfloat</span> <span class="n">height</span><span class="p">;</span>
  <span class="n">gfloat</span> <span class="n">confidence</span><span class="p">;</span>
  <span class="n">gint</span>   <span class="n">class_id</span><span class="p">;</span>
<span class="p">}</span> <span class="n">GstDetection</span><span class="p">;</span>

<span class="k">typedef</span> <span class="k">struct</span> <span class="nc">_GstDetectionsMeta</span> <span class="p">{</span>
  <span class="n">GstMeta</span> <span class="n">meta</span><span class="p">;</span>
  <span class="n">GArray</span> <span class="o">*</span><span class="n">detections</span><span class="p">;</span>   <span class="cm">/* each entry is a GstDetection */</span>
<span class="p">}</span> <span class="n">GstDetectionsMeta</span><span class="p">;</span>
</code></pre></div></div> <p>The two helpers we wrap around the GStreamer API are:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">GstDetectionsMeta</span> <span class="o">*</span><span class="nf">gst_buffer_add_detections_meta</span><span class="p">(</span><span class="n">GstBuffer</span> <span class="o">*</span><span class="n">buffer</span><span class="p">);</span>
<span class="n">GstDetectionsMeta</span> <span class="o">*</span><span class="nf">gst_buffer_get_detections_meta</span><span class="p">(</span><span class="n">GstBuffer</span> <span class="o">*</span><span class="n">buffer</span><span class="p">);</span>
</code></pre></div></div> <p>A few lessons I have the bruises from:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">transform_func</code> is not optional.</strong> The first time I dropped a <code class="language-plaintext highlighter-rouge">queue</code> between <code class="language-plaintext highlighter-rouge">yolonnx</code> and a downstream consumer, my meta quietly vanished. The fix is to register a transform callback with <code class="language-plaintext highlighter-rouge">gst_meta_register</code> so when GStreamer copies a buffer, it knows how to copy your meta along with it. We implement it up front, copying the <code class="language-plaintext highlighter-rouge">GArray</code> contents into a fresh meta on the destination buffer—learn from the bruises, do it on day one.</li> <li><strong>Lifetime.</strong> Don’t store pointers into the <strong>mapped</strong> pixel memory inside meta. Plain coordinates and scores are fine because they live in the meta’s own <code class="language-plaintext highlighter-rouge">GArray</code>. If downstream might resize, store coordinates in <strong>normalised</strong> <code class="language-plaintext highlighter-rouge">[0, 1]</code> form, or clearly document that the numbers only make sense at the current buffer’s width and height.</li> <li><strong>Threading.</strong> Inference is slow. Some day we’ll talk about workers and queues and keeping <strong>PTS</strong> aligned with delayed results; for today, keep it single-threaded until the logic is boringly correct.</li> </ul> <h2 id="putting-it-together-in-the-repo">Putting It Together in the Repo</h2> <p>The full CMake setup and the <code class="language-plaintext highlighter-rouge">GstMeta</code> registration live in our companion repo <strong><a href="https://github.com/rosemary-crypto/build-with-gstreamer">build-with-gstreamer</a></strong> on the <code class="language-plaintext highlighter-rouge">lesson-03-yolo-onnx</code> branch, sitting next to the <code class="language-plaintext highlighter-rouge">lesson-02-plugins</code> branch from Lesson 2 so you can diff between them.</p> <p>The layout looks like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gst/yolonnx/
  include/
    gstyolonnx.h           # the GstVideoFilter subclass
    gstdetectionsmeta.h    # the meta API
  src/
    gstyolonnx.cpp         # set_info + transform_frame_ip + inference
    gstdetectionsmeta.cpp  # register + add/get/transform helpers
    plugin.cpp             # GST_PLUGIN_DEFINE
  CMakeLists.txt           # finds ONNX Runtime, builds libgstyolonnx.so

gst-apps/lesson-03-yolo-onnx/
  src/main.cpp             # filesrc ! decodebin ! videoconvert ! yolonnx ! autovideosink
  CMakeLists.txt
</code></pre></div></div> <p>The article you’re reading is the map; the branch is the ground truth.</p> <h2 id="time-to-experiment">Time to Experiment!</h2> <p>Now that you have the story end-to-end, here are some challenges I’d try if I were sitting next to you at the keyboard:</p> <h3 id="1-run-it-with-no-model">1. Run It With No Model</h3> <p>Launch the pipeline with no <code class="language-plaintext highlighter-rouge">model-path</code> set. The synthetic moving box should appear and <code class="language-plaintext highlighter-rouge">gst_buffer_get_detections_meta</code> should return non-empty results—a fast way to confirm your draw path and meta wiring without an ONNX file.</p> <h3 id="2-meta-only-no-drawing">2. Meta Only, No Drawing</h3> <p>Set <code class="language-plaintext highlighter-rouge">draw-boxes=false attach-meta=true</code> and build a tiny <code class="language-plaintext highlighter-rouge">identity</code>-style element downstream that only reads <code class="language-plaintext highlighter-rouge">GstDetectionsMeta</code> and prints class ids. You’ll know you understand meta when that works.</p> <h3 id="3-survive-a-queue">3. Survive a <code class="language-plaintext highlighter-rouge">queue</code></h3> <p>Insert a <code class="language-plaintext highlighter-rouge">queue</code> between <code class="language-plaintext highlighter-rouge">yolonnx</code> and your downstream consumer and confirm the meta still arrives. It should—because we wired up <code class="language-plaintext highlighter-rouge">transform_func</code> from day one—but seeing the test pass is a nice confirmation that your meta is actually transform-safe.</p> <h3 id="4-log-timing-like-we-did-for-fps">4. Log Timing Like We Did for FPS</h3> <p>Reuse the clock tricks from the frame monitor article: wrap the <code class="language-plaintext highlighter-rouge">Session::Run</code> call inside <code class="language-plaintext highlighter-rouge">transform_frame_ip</code> and measure how long inference takes per frame at 720p versus 1080p. The numbers tell you when you actually need a GPU provider.</p> <h3 id="5-debug-the-usual-way">5. Debug the Usual Way</h3> <p>When boxes go sideways:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GST_DEBUG</span><span class="o">=</span>yolonnx:5
</code></pre></div></div> <p>…and uncomment the per-detection <code class="language-plaintext highlighter-rouge">g_print</code> line in <code class="language-plaintext highlighter-rouge">run_detection</code> to see class ids, scores, and box coordinates streaming past in real time. Both tools want to help you if you ask loudly enough in debug logs.</p> <h2 id="conclusion">Conclusion</h2> <p>Today we connected the dots from <strong>Phase 1</strong> to <strong>Phase 2</strong>: take the standalone YOLO ONNX detector from the <a href="/blog/2026/yolo-onnx-standalone-detector-cpp/">previous article</a>, wrap it in a <strong><code class="language-plaintext highlighter-rouge">GstVideoFilter</code></strong>-based plugin that draws boxes on the video, and attach structured results with <strong><code class="language-plaintext highlighter-rouge">GstMeta</code></strong> so the rest of the pipeline can grow without re-running the network on every hop.</p> <p>The two pieces I want you to take away from this one are the <strong><code class="language-plaintext highlighter-rouge">draw-boxes</code> / <code class="language-plaintext highlighter-rouge">attach-meta</code> split</strong>—because it shows the cost of inference paying for two different downstream stories at once—and the <strong><code class="language-plaintext highlighter-rouge">transform_func</code></strong> detail that keeps your meta alive across <code class="language-plaintext highlighter-rouge">queue</code>s. Skip either of those and the next phase gets harder than it needs to be.</p> <p>Stay tuned for <strong>Phase 3</strong> where we finally start doing something with those boxes besides staring at them—object tracking. 🚀</p>]]></content><author><name></name></author><category term="tutorials"/><category term="GStreamer,"/><category term="development,"/><category term="C++,"/><category term="YOLO,"/><category term="ONNX,"/><category term="ONNX"/><category term="Runtime,"/><category term="deep"/><category term="learning,"/><category term="video"/><category term="analytics,"/><category term="GstMeta"/><summary type="html"><![CDATA[Wrap the standalone YOLO ONNX detector from the previous article in a GStreamer plugin, draw boxes on the video, and—the part that really matters—attach detections to each buffer with GstMeta so the rest of the pipeline can reuse them.]]></summary></entry><entry><title type="html">Proving YOLO on ONNX in Standalone C++ — Before We Plug It Into GStreamer</title><link href="https://rosemary-crypto.github.io/blog/2026/yolo-onnx-standalone-detector-cpp/" rel="alternate" type="text/html" title="Proving YOLO on ONNX in Standalone C++ — Before We Plug It Into GStreamer"/><published>2026-04-30T12:00:00+00:00</published><updated>2026-04-30T12:00:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2026/yolo-onnx-standalone-detector-cpp</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2026/yolo-onnx-standalone-detector-cpp/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="a-quick-detour-before-lesson-3">A Quick Detour Before Lesson 3</h2> <p>In our <a href="/blog/2025/understanding-gstreamer-buffers-and-plugin-development/">last article</a> we built a frame monitor and got comfortable with buffers, pads, and the chain function. The next thing on our roadmap is <strong>Phase 2</strong>—a YOLO detector wrapped in a real GStreamer plugin.</p> <p>But before I drop a neural network into a pipeline I want to know one thing for certain: <strong>does the model even work outside GStreamer?</strong> That is the entire point of this short detour. We’ll export a YOLO checkpoint to ONNX, run it from C++ with ONNX Runtime on a few still images, and only then—in the next article—plug the same code into a GStreamer element.</p> <p>If the boxes are wrong in a tiny console app, they’ll still be wrong inside GStreamer, only by then you’ll also be debugging caps negotiation and clocking at the same time. Trust me on this one.</p> <h2 id="why-onnx-and-onnx-runtime">Why ONNX and ONNX Runtime?</h2> <p>When I first tried to jam TensorFlow or PyTorch directly into a GStreamer element, I spent more time fighting build systems than writing multimedia code. Think of <strong>ONNX</strong> as a <strong>lingua franca</strong> for models: you train or download weights in Python, you export once to a frozen graph, and your C++ code just loads that file.</p> <p><strong>ONNX Runtime</strong> is the engine on the other side. It has a reasonably friendly C++ API, it runs well on CPU, and when you’re ready you can turn on GPU execution providers without rewriting your whole pipeline.</p> <p>In production I usually reach for <strong>TensorRT</strong> on NVIDIA hardware, or <strong>TensorFlow</strong> where our tooling already lives there—but for this series I picked <strong>ONNX Runtime</strong> on purpose so you can follow along on a normal laptop.</p> <h2 id="step-1--getting-a-model-into-onnx">Step 1 — Getting a Model into ONNX</h2> <p>From a Python environment where you already have <code class="language-plaintext highlighter-rouge">ultralytics</code> installed, exporting feels almost too easy:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Example: a tiny YOLO11—swap in yolov8n.pt or whatever you trained</span>
yolo <span class="nb">export </span><span class="nv">model</span><span class="o">=</span>yolo11n.pt <span class="nv">format</span><span class="o">=</span>onnx <span class="nv">opset</span><span class="o">=</span>12 <span class="nv">simplify</span><span class="o">=</span>True
</code></pre></div></div> <p>You should see something like <code class="language-plaintext highlighter-rouge">yolo11n.onnx</code> show up next to your checkpoint. Before you write a single line of C++, do yourself a favor and open that file in <a href="https://netron.app/">Netron</a>. I can’t count how many hours I’ve lost assuming the input was named <code class="language-plaintext highlighter-rouge">images</code> when the exporter quietly picked something else, or guessing output shapes from an old blog post while my actual graph had already moved on.</p> <h3 id="what-are-we-actually-decoding">What Are We Actually Decoding?</h3> <p>Exported YOLO graphs usually hand you a <strong>big pile of candidate boxes</strong>—think of it like a sorting room where every parcel looks important until you read the labels. In code, the dance is almost always:</p> <ol> <li>Maybe apply <strong>sigmoid</strong> (depends whether the export already folded that in).</li> <li>Throw away predictions below a <strong>score threshold</strong>.</li> <li>Turn whatever format the head uses (centre and size, corners, etc.) into plain <strong>x1, y1, x2, y2</strong> in your image space.</li> <li>Run <strong>NMS</strong> so the same car doesn’t generate fifteen overlapping boxes.</li> </ol> <p>Keep your thresholds and NMS in <strong>one shared module</strong>. You’ll thank yourself later when the standalone tester and the GStreamer element both call the same functions.</p> <p>Here is one more gotcha I learned the hard way: some Ultralytics exports give you an <strong>end-to-end</strong> graph where NMS already lives inside ONNX, and the output looks like a tidy <code class="language-plaintext highlighter-rouge">[1, N, 6]</code> table of boxes and scores. Others give you the <strong>raw head</strong>—a wide tensor like <code class="language-plaintext highlighter-rouge">[1, 84, 8400]</code> (YOLOv8) where four box coordinates and the class scores are all stacked along one axis. Treat the layout as <strong>version-specific</strong>, wire your decoder after you look at <em>your</em> file, and pin the <code class="language-plaintext highlighter-rouge">ultralytics</code> version in a comment when it works. Upgrades are wonderful until they silently change a tensor name.</p> <h2 id="step-2--the-standalone-c-recipe">Step 2 — The Standalone C++ Recipe</h2> <p>The recipe is always the same story:</p> <ol> <li><strong>Resize or letterbox</strong> into the network’s expected height and width (match how the model was trained if you care about accuracy).</li> <li><strong>Normalise</strong> to <code class="language-plaintext highlighter-rouge">float32</code> in <strong>NCHW</strong> order—again, <strong>match the export</strong>, don’t assume <code class="language-plaintext highlighter-rouge">[0,1]</code> if your graph expects something else.</li> <li>Build an <strong><code class="language-plaintext highlighter-rouge">Ort::Value</code></strong> tensor and call <strong><code class="language-plaintext highlighter-rouge">Session::Run</code></strong>.</li> <li>Feed the output tensors into your <strong>decoder + NMS</strong> and end up with something like:</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">Detection</span> <span class="p">{</span>
  <span class="kt">float</span> <span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">width</span><span class="p">,</span> <span class="n">height</span><span class="p">;</span>
  <span class="kt">float</span> <span class="n">confidence</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">class_id</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>Here is a tiny skeleton of the Runtime call—error handling omitted so we can see the forest for the trees, but please add real checks in your repo:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;onnxruntime_cxx_api.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
</span>
<span class="k">static</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">Detection</span><span class="o">&gt;</span> <span class="n">run_yolo_session</span><span class="p">(</span>
    <span class="n">Ort</span><span class="o">::</span><span class="n">Session</span><span class="o">&amp;</span> <span class="n">session</span><span class="p">,</span>
    <span class="k">const</span> <span class="n">Ort</span><span class="o">::</span><span class="n">MemoryInfo</span><span class="o">&amp;</span> <span class="n">mem_info</span><span class="p">,</span>
    <span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">nchw_input</span><span class="p">,</span>  <span class="c1">// length = 1*3*H*W</span>
    <span class="kt">int</span> <span class="n">H</span><span class="p">,</span> <span class="kt">int</span> <span class="n">W</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">Ort</span><span class="o">::</span><span class="n">AllocatorWithDefaultOptions</span> <span class="n">alloc</span><span class="p">;</span>
  <span class="k">auto</span> <span class="n">in_name</span> <span class="o">=</span> <span class="n">session</span><span class="p">.</span><span class="n">GetInputNameAllocated</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">alloc</span><span class="p">);</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">const</span> <span class="kt">char</span><span class="o">*&gt;</span> <span class="n">in_names</span><span class="p">{</span><span class="n">in_name</span><span class="p">.</span><span class="n">get</span><span class="p">()};</span>

  <span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="kt">int64_t</span><span class="p">,</span> <span class="mi">4</span><span class="o">&gt;</span> <span class="n">shape</span><span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">H</span><span class="p">,</span> <span class="n">W</span><span class="p">};</span>
  <span class="n">Ort</span><span class="o">::</span><span class="n">Value</span> <span class="n">in_tensor</span> <span class="o">=</span> <span class="n">Ort</span><span class="o">::</span><span class="n">Value</span><span class="o">::</span><span class="n">CreateTensor</span><span class="o">&lt;</span><span class="kt">float</span><span class="o">&gt;</span><span class="p">(</span>
      <span class="n">mem_info</span><span class="p">,</span> <span class="k">const_cast</span><span class="o">&lt;</span><span class="kt">float</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">nchw_input</span><span class="p">),</span> <span class="mi">1</span> <span class="o">*</span> <span class="mi">3</span> <span class="o">*</span> <span class="n">H</span> <span class="o">*</span> <span class="n">W</span><span class="p">,</span>
      <span class="n">shape</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span> <span class="n">shape</span><span class="p">.</span><span class="n">size</span><span class="p">());</span>

  <span class="k">auto</span> <span class="n">out_name</span> <span class="o">=</span> <span class="n">session</span><span class="p">.</span><span class="n">GetOutputNameAllocated</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">alloc</span><span class="p">);</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">const</span> <span class="kt">char</span><span class="o">*&gt;</span> <span class="n">out_names</span><span class="p">{</span><span class="n">out_name</span><span class="p">.</span><span class="n">get</span><span class="p">()};</span>

  <span class="k">auto</span> <span class="n">outputs</span> <span class="o">=</span> <span class="n">session</span><span class="p">.</span><span class="n">Run</span><span class="p">(</span><span class="n">Ort</span><span class="o">::</span><span class="n">RunOptions</span><span class="p">{</span><span class="nb">nullptr</span><span class="p">},</span>
                             <span class="n">in_names</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span> <span class="o">&amp;</span><span class="n">in_tensor</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span>
                             <span class="n">out_names</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span> <span class="mi">1</span><span class="p">);</span>
  <span class="c1">// Parse outputs[0] into Detection vector + NMS ...</span>
  <span class="k">return</span> <span class="p">{};</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="decoding-a-yolov8-head">Decoding a YOLOv8 Head</h3> <p>For a typical YOLOv8 export the output tensor is shaped <code class="language-plaintext highlighter-rouge">[1, 84, 8400]</code>—84 is <code class="language-plaintext highlighter-rouge">4 box coords + 80 class scores</code>, and 8400 is the number of candidate anchors after the model’s internal grid math. The decode loop looks like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Pseudocode shape: out_data is [1, num_attrs, num_anchors]</span>
<span class="k">const</span> <span class="kt">int</span> <span class="n">num_classes</span> <span class="o">=</span> <span class="n">num_attrs</span> <span class="o">-</span> <span class="mi">4</span><span class="p">;</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">num_anchors</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">float</span> <span class="n">cx</span> <span class="o">=</span> <span class="n">out_data</span><span class="p">[</span><span class="mi">0</span> <span class="o">*</span> <span class="n">num_anchors</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
  <span class="kt">float</span> <span class="n">cy</span> <span class="o">=</span> <span class="n">out_data</span><span class="p">[</span><span class="mi">1</span> <span class="o">*</span> <span class="n">num_anchors</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
  <span class="kt">float</span> <span class="n">bw</span> <span class="o">=</span> <span class="n">out_data</span><span class="p">[</span><span class="mi">2</span> <span class="o">*</span> <span class="n">num_anchors</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
  <span class="kt">float</span> <span class="n">bh</span> <span class="o">=</span> <span class="n">out_data</span><span class="p">[</span><span class="mi">3</span> <span class="o">*</span> <span class="n">num_anchors</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>

  <span class="kt">int</span>   <span class="n">best_class</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
  <span class="kt">float</span> <span class="n">best_score</span> <span class="o">=</span> <span class="mf">0.0</span><span class="n">f</span><span class="p">;</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">c</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">c</span> <span class="o">&lt;</span> <span class="n">num_classes</span><span class="p">;</span> <span class="o">++</span><span class="n">c</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">s</span> <span class="o">=</span> <span class="n">out_data</span><span class="p">[(</span><span class="mi">4</span> <span class="o">+</span> <span class="n">c</span><span class="p">)</span> <span class="o">*</span> <span class="n">num_anchors</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">s</span> <span class="o">&gt;</span> <span class="n">best_score</span><span class="p">)</span> <span class="p">{</span> <span class="n">best_score</span> <span class="o">=</span> <span class="n">s</span><span class="p">;</span> <span class="n">best_class</span> <span class="o">=</span> <span class="n">c</span><span class="p">;</span> <span class="p">}</span>
  <span class="p">}</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">best_score</span> <span class="o">&lt;</span> <span class="n">conf_threshold</span><span class="p">)</span> <span class="k">continue</span><span class="p">;</span>

  <span class="c1">// (cx, cy, w, h) in network coords -&gt; (x, y, w, h) in original frame</span>
  <span class="c1">// ... scale by frame_w / net_w, frame_h / net_h ...</span>
  <span class="c1">// ... append to candidates, then NMS by class_id.</span>
<span class="p">}</span>
</code></pre></div></div> <p>Once you have your candidate vector, sort by confidence and run a simple per-class IoU pass to drop overlapping boxes. If you already depend on <strong>OpenCV</strong>, <code class="language-plaintext highlighter-rouge">cv::dnn::NMSBoxes</code> saves you from reimplementing this for the tenth time in your life.</p> <h3 id="a-word-on-dependencies">A Word on Dependencies</h3> <p>On Ubuntu I usually pull <strong>ONNX Runtime</strong> from Microsoft’s packages or vendor a known-good tarball into the repo so CI and my laptop agree. <strong>OpenCV</strong> is optional but lovely for the standalone tester—you get image loading, resizing, drawing, and <code class="language-plaintext highlighter-rouge">NMSBoxes</code> for free. Inside the GStreamer plugin we’ll keep the dependency surface smaller and draw rectangles with plain pixel loops; for now, the standalone tester is allowed to be cushy.</p> <h2 id="step-3--a-tiny-tester">Step 3 — A Tiny Tester</h2> <p>The shape of the standalone program is intentionally boring:</p> <ol> <li>Load a JPEG with OpenCV (or any image library).</li> <li>Resize to your model’s input size (<code class="language-plaintext highlighter-rouge">640x640</code> for typical YOLO exports).</li> <li>Normalize to <code class="language-plaintext highlighter-rouge">float32 / 255.0</code> in NCHW order, RGB.</li> <li>Run the session.</li> <li>Decode + NMS.</li> <li>Draw boxes on the original image and save to disk.</li> </ol> <p>Open the output. Either the box lands on the dog, or it doesn’t. That is the whole game. If it doesn’t land where you expect, suspect (in this order): <strong>input channel order</strong> (BGR vs RGB), <strong>normalization</strong> (<code class="language-plaintext highlighter-rouge">/255</code> vs <code class="language-plaintext highlighter-rouge">/127.5 - 1</code>), <strong>box format</strong> (cx,cy,w,h vs x1,y1,x2,y2), and finally <strong>the export itself</strong>. About 90% of my “the model is broken” moments were one of those four.</p> <h2 id="what-comes-next">What Comes Next</h2> <p>Once your standalone tester puts boxes in the right places on still images, you are holding the same functions you’ll call from a GStreamer chain function—only the <strong>source</strong> of the pixels changes. In the next article we’ll wrap exactly this code into a <code class="language-plaintext highlighter-rouge">GstVideoFilter</code> plugin called <code class="language-plaintext highlighter-rouge">yolonnx</code>, draw boxes directly on the video, and attach the detections to each buffer as <code class="language-plaintext highlighter-rouge">GstMeta</code> so downstream elements (trackers, recorders, MQTT bridges) can read them without re-running inference.</p> <p>The companion repo is <strong><a href="https://github.com/rosemary-crypto/build-with-gstreamer">build-with-gstreamer</a></strong>. The standalone work for this article lives next to the plugin code so you can diff the two and see how little has to change.</p> <p>See you in <a href="/blog/2026/gstreamer-yolo-onnx-plugin-detections-metadata/">Lesson 3</a> where we finally put this on a conveyor belt. 🚀</p>]]></content><author><name></name></author><category term="tutorials"/><category term="C++,"/><category term="YOLO,"/><category term="ONNX,"/><category term="ONNX"/><category term="Runtime,"/><category term="deep"/><category term="learning,"/><category term="video"/><category term="analytics"/><summary type="html"><![CDATA[A short detour between Lesson 2 and Lesson 3—export a YOLO model to ONNX, run it from C++ with ONNX Runtime, and convince yourself the boxes are right before any GStreamer code touches the problem.]]></summary></entry><entry><title type="html">Understanding GStreamer Buffers and Plugin Development - Building Blocks for Video Analytics</title><link href="https://rosemary-crypto.github.io/blog/2025/understanding-gstreamer-buffers-and-plugin-development/" rel="alternate" type="text/html" title="Understanding GStreamer Buffers and Plugin Development - Building Blocks for Video Analytics"/><published>2025-03-14T23:00:00+00:00</published><updated>2025-03-14T23:00:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2025/understanding-gstreamer-buffers-and-plugin-development</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2025/understanding-gstreamer-buffers-and-plugin-development/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="the-journey-ahead">The Journey Ahead</h2> <p>Remember how in our last article I mentioned rebuilding an AI video analytics platform? Well, I’m excited to tell you that throughout this series, we’re going to build one together! But before we dive into the complex stuff like AI integration and real-time analytics, we need to understand some fundamental concepts. Today, we’re starting with something I wish someone had explained to me when I first started: GStreamer buffers and how to create our first plugin.</p> <h3 id="our-roadmap">Our Roadmap</h3> <p>Let me share what we’re going to build over the next few articles:</p> <ol> <li><strong>Phase 1 (Today)</strong>: We’ll create a frame monitor plugin - think of it as our first step into the GStreamer plugin development world. It’s simple but teaches us crucial concepts.</li> <li><strong>Phase 2</strong>: We’ll build plugins for AI model integration and inference.</li> <li><strong>Phase 3</strong>: We’ll add object tracking capabilities.</li> <li><strong>Phase 4</strong>: We’ll implement event detection and real-time analytics.</li> <li><strong>Phase 5</strong>: We’ll wrap it up with data distribution and visualization.</li> </ol> <h2 id="understanding-gstreamer-buffers">Understanding GStreamer Buffers</h2> <p>When I first started with GStreamer, buffers were one of those concepts that took me a while to grasp. Think of a buffer as a container carrying our precious cargo (video frames) through the pipeline. It’s not just the raw data though - it comes with a lot of useful information about timing, flags, and other metadata.</p> <h3 id="whats-in-a-buffer">What’s in a Buffer?</h3> <p>A GStreamer buffer is like a smart package with:</p> <ul> <li>The actual frame data</li> <li>Timing information (PTS - presentation timestamp)</li> <li>Duration</li> <li>Offset information</li> <li>Special flags telling us about the content</li> </ul> <h2 id="anatomy-of-a-gstreamer-plugin">Anatomy of a GStreamer Plugin</h2> <p>Now that we understand buffers, let’s create something useful with them. We’re going to build a frame monitor plugin that will help us understand what’s happening in our video pipeline.</p> <h3 id="the-element-structure">The Element Structure</h3> <p>First, let’s look at our element’s structure:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">_GstFrameMonitor</span> <span class="p">{</span>
    <span class="n">GstElement</span> <span class="n">parent</span><span class="p">;</span>      <span class="c1">// Inherit from GstElement</span>
    <span class="n">GstPad</span> <span class="o">*</span><span class="n">sinkpad</span><span class="p">;</span>       <span class="c1">// Input pad</span>
    <span class="n">GstPad</span> <span class="o">*</span><span class="n">srcpad</span><span class="p">;</span>        <span class="c1">// Output pad</span>

    <span class="n">GstVideoInfo</span> <span class="n">video_info</span><span class="p">;</span>  <span class="c1">// Video format information</span>
    <span class="n">guint64</span> <span class="n">frame_count</span><span class="p">;</span>      <span class="c1">// Frame counter</span>
    <span class="n">GstClockTime</span> <span class="n">last_pts</span><span class="p">;</span>    <span class="c1">// For FPS calculation</span>
    <span class="n">gdouble</span> <span class="n">fps</span><span class="p">;</span>              <span class="c1">// Current FPS</span>
<span class="p">};</span>
</code></pre></div></div> <p>This structure is like our element’s DNA. The <code class="language-plaintext highlighter-rouge">parent</code> field inherits from <code class="language-plaintext highlighter-rouge">GstElement</code>, giving us all the basic GStreamer element functionality. Think of it like inheriting traits from a parent class in object-oriented programming.</p> <h3 id="pad-templates-defining-our-elements-interface">Pad Templates: Defining Our Element’s Interface</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">GstStaticPadTemplate</span> <span class="n">sink_template</span> <span class="o">=</span> <span class="n">GST_STATIC_PAD_TEMPLATE</span><span class="p">(</span>
    <span class="s">"sink"</span><span class="p">,</span>
    <span class="n">GST_PAD_SINK</span><span class="p">,</span>
    <span class="n">GST_PAD_ALWAYS</span><span class="p">,</span>
    <span class="n">GST_STATIC_CAPS</span><span class="p">(</span><span class="n">GST_VIDEO_CAPS_MAKE</span><span class="p">(</span><span class="s">"{ I420, NV12, RGB, BGR, RGBA, BGRA }"</span><span class="p">))</span>
<span class="p">);</span>
</code></pre></div></div> <p>Pad templates are like a contract that specifies what kind of data our element can handle. Here, we’re telling GStreamer:</p> <ul> <li>Our element has a sink pad named “sink”</li> <li>This pad is always present (not dynamic)</li> <li>It can handle specific video formats (I420, NV12, RGB, etc.)</li> </ul> <h3 id="the-chain-function-where-data-flows">The Chain Function: Where Data Flows</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">GstFlowReturn</span>
<span class="nf">gst_frame_monitor_chain</span><span class="p">(</span><span class="n">GstPad</span><span class="o">*</span> <span class="n">pad</span><span class="p">,</span> <span class="n">GstObject</span><span class="o">*</span> <span class="n">parent</span><span class="p">,</span> <span class="n">GstBuffer</span><span class="o">*</span> <span class="n">buf</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">GstFrameMonitor</span><span class="o">*</span> <span class="n">filter</span> <span class="o">=</span> <span class="n">GST_FRAME_MONITOR</span><span class="p">(</span><span class="n">parent</span><span class="p">);</span>
    <span class="n">GstMapInfo</span> <span class="n">map</span><span class="p">;</span>
</code></pre></div></div> <p>The chain function is where the real work happens. Think of it as a conveyor belt worker in a factory:</p> <ol> <li>It receives a buffer (<code class="language-plaintext highlighter-rouge">buf</code>) containing a video frame</li> <li>Maps the buffer to access its contents</li> <li>Analyzes the frame data</li> <li>Passes the buffer downstream</li> </ol> <h3 id="buffer-mapping-safe-memory-access">Buffer Mapping: Safe Memory Access</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">gst_buffer_map</span><span class="p">(</span><span class="n">buf</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">map</span><span class="p">,</span> <span class="n">GST_MAP_READ</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">GST_ERROR_OBJECT</span><span class="p">(</span><span class="n">filter</span><span class="p">,</span> <span class="s">"Failed to map buffer"</span><span class="p">);</span>
        <span class="k">return</span> <span class="n">GST_FLOW_ERROR</span><span class="p">;</span>
    <span class="p">}</span>
</code></pre></div></div> <p>Buffer mapping is crucial for safe memory access. It’s like checking out a book from a library:</p> <ol> <li>You request access (map the buffer)</li> <li>Get a safe way to read it (map.data)</li> <li>Return it when done (unmap)</li> </ol> <h3 id="timing-and-fps-calculation">Timing and FPS Calculation</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">if</span> <span class="p">(</span><span class="n">GST_CLOCK_TIME_IS_VALID</span><span class="p">(</span><span class="n">filter</span><span class="o">-&gt;</span><span class="n">last_pts</span><span class="p">)</span> <span class="o">&amp;&amp;</span>
        <span class="n">GST_CLOCK_TIME_IS_VALID</span><span class="p">(</span><span class="n">pts</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">GstClockTimeDiff</span> <span class="n">diff</span> <span class="o">=</span> <span class="n">GST_CLOCK_DIFF</span><span class="p">(</span><span class="n">filter</span><span class="o">-&gt;</span><span class="n">last_pts</span><span class="p">,</span> <span class="n">pts</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">diff</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">filter</span><span class="o">-&gt;</span><span class="n">fps</span> <span class="o">=</span> <span class="n">GST_SECOND</span> <span class="o">/</span> <span class="p">(</span><span class="n">gdouble</span><span class="p">)</span><span class="n">diff</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>
</code></pre></div></div> <p>This code calculates the frames per second by:</p> <ol> <li>Checking if we have valid timestamps</li> <li>Calculating the time difference between frames</li> <li>Converting this to FPS using GStreamer’s time units</li> </ol> <h3 id="event-handling-control-flow-in-the-pipeline">Event Handling: Control Flow in the Pipeline</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">gboolean</span>
<span class="nf">gst_frame_monitor_sink_event</span><span class="p">(</span><span class="n">GstPad</span><span class="o">*</span> <span class="n">pad</span><span class="p">,</span> <span class="n">GstObject</span><span class="o">*</span> <span class="n">parent</span><span class="p">,</span> <span class="n">GstEvent</span><span class="o">*</span> <span class="n">event</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">switch</span> <span class="p">(</span><span class="n">GST_EVENT_TYPE</span><span class="p">(</span><span class="n">event</span><span class="p">))</span> <span class="p">{</span>
        <span class="k">case</span> <span class="n">GST_EVENT_CAPS</span><span class="p">:</span> <span class="p">{</span>
            <span class="n">GstCaps</span><span class="o">*</span> <span class="n">caps</span><span class="p">;</span>
            <span class="n">gst_event_parse_caps</span><span class="p">(</span><span class="n">event</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">caps</span><span class="p">);</span>
            <span class="c1">// Handle caps...</span>
            <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>Events are GStreamer’s way of sending control information through the pipeline. Think of them as traffic signals in our data highway:</p> <ul> <li>CAPS events tell us about the format of incoming data</li> <li>EOS events signal the end of the stream</li> <li>SEGMENT events provide timing information</li> </ul> <h3 id="understanding-buffer-flags">Understanding Buffer Flags</h3> <p>When we receive a buffer, it comes with flags that tell us important information:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">guint</span> <span class="n">flags</span> <span class="o">=</span> <span class="n">GST_BUFFER_FLAGS</span><span class="p">(</span><span class="n">buf</span><span class="p">);</span>
<span class="n">gboolean</span> <span class="n">is_keyframe</span> <span class="o">=</span> <span class="o">!</span><span class="p">(</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="n">GST_BUFFER_FLAG_DELTA_UNIT</span><span class="p">);</span>
<span class="n">gboolean</span> <span class="n">is_header</span> <span class="o">=</span> <span class="o">!!</span><span class="p">(</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="n">GST_BUFFER_FLAG_HEADER</span><span class="p">);</span>
</code></pre></div></div> <p>These flags are like metadata tags that tell us:</p> <ul> <li>If this is a keyframe (important for video compression)</li> <li>If it’s a header frame (contains stream configuration)</li> <li>If the frame can be dropped (for performance)</li> <li>If there’s a gap in the data</li> </ul> <h3 id="element-states-and-lifecycle">Element States and Lifecycle</h3> <p>Our element goes through several states:</p> <ol> <li><strong>NULL</strong>: Initial state, like a car with the engine off</li> <li><strong>READY</strong>: Resources allocated, engine started</li> <li><strong>PAUSED</strong>: Ready to process data, car in gear</li> <li><strong>PLAYING</strong>: Actively processing frames, car moving</li> </ol> <p>Each state transition is handled automatically by GStreamer, but we can hook into them if needed.</p> <h2 id="whats-next">What’s Next?</h2> <p>Now that we understand how to create a basic plugin and handle video frames, we’re ready to move on to more exciting things. In our next article, we’ll:</p> <ol> <li>Add AI model integration</li> <li>Learn about buffer memory management for efficient processing</li> <li>Implement proper error handling and recovery</li> </ol> <p>The complete code for this lesson is available in our <a href="https://github.com/rosemary-crypto/build-with-gstreamer">GitHub repository</a> under the <code class="language-plaintext highlighter-rouge">lesson-02-plugins</code> branch.</p> <h2 id="time-to-experiment">Time to Experiment!</h2> <p>Now that you understand the basics of plugin development, here are some fun challenges you can try to deepen your understanding:</p> <h3 id="1-add-more-metrics">1. Add More Metrics</h3> <p>Try extending the frame monitor to calculate and display:</p> <ul> <li>Average frame size over time</li> <li>Min/Max FPS</li> <li>Frame size distribution</li> <li>Time spent processing each frame</li> </ul> <p>Here’s a hint for calculating processing time:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">GstClockTime</span> <span class="n">start_time</span> <span class="o">=</span> <span class="n">gst_clock_get_time</span><span class="p">(</span><span class="n">GST_ELEMENT_CLOCK</span><span class="p">(</span><span class="n">filter</span><span class="p">));</span>
<span class="c1">// Your processing code here</span>
<span class="n">GstClockTime</span> <span class="n">end_time</span> <span class="o">=</span> <span class="n">gst_clock_get_time</span><span class="p">(</span><span class="n">GST_ELEMENT_CLOCK</span><span class="p">(</span><span class="n">filter</span><span class="p">));</span>
<span class="n">GstClockTimeDiff</span> <span class="n">processing_time</span> <span class="o">=</span> <span class="n">GST_CLOCK_DIFF</span><span class="p">(</span><span class="n">start_time</span><span class="p">,</span> <span class="n">end_time</span><span class="p">);</span>
</code></pre></div></div> <h3 id="2-play-with-buffer-flags">2. Play with Buffer Flags</h3> <p>Our plugin currently just reads buffer flags, but you can try:</p> <ul> <li>Marking frames that meet certain criteria (e.g., frames larger than average)</li> <li>Adding custom metadata to buffers</li> <li>Implementing a simple frame dropping mechanism for frames with the <code class="language-plaintext highlighter-rouge">GST_BUFFER_FLAG_DROPPABLE</code> flag</li> </ul> <h3 id="3-add-properties">3. Add Properties</h3> <p>Make the plugin configurable by adding properties:</p> <ul> <li>A boolean to enable/disable FPS calculation</li> <li>A threshold for minimum frame size logging</li> <li>An interval for how often to print statistics</li> </ul> <p>Here’s a starting point:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">enum</span> <span class="p">{</span>
    <span class="n">PROP_0</span><span class="p">,</span>
    <span class="n">PROP_SHOW_FPS</span><span class="p">,</span>
    <span class="n">PROP_MIN_FRAME_SIZE</span><span class="p">,</span>
    <span class="n">PROP_PRINT_INTERVAL</span>
<span class="p">};</span>
</code></pre></div></div> <h3 id="4-experiment-with-different-video-sources">4. Experiment with Different Video Sources</h3> <p>Try your plugin with:</p> <ul> <li>Different video files (MP4, MKV, AVI)</li> <li>Live camera input using <code class="language-plaintext highlighter-rouge">v4l2src</code></li> <li>RTSP streams</li> <li>Different video formats and resolutions</li> </ul> <p>Here’s a command to test with your webcam:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gst-launch-1.0 v4l2src <span class="o">!</span> videoconvert <span class="o">!</span> framemonitor <span class="o">!</span> autovideosink
</code></pre></div></div> <h3 id="5-add-visual-feedback">5. Add Visual Feedback</h3> <p>Instead of just printing information, try:</p> <ul> <li>Drawing the FPS directly on the video frames</li> <li>Adding a timestamp overlay</li> <li>Creating a simple histogram of frame sizes</li> </ul> <p>Remember, the best way to learn is by breaking things and fixing them. Don’t be afraid to experiment - GStreamer’s debug system will help you understand what’s going wrong:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GST_DEBUG</span><span class="o">=</span>framemonitor:5
</code></pre></div></div> <h2 id="conclusion">Conclusion</h2> <p>Today we’ve learned how to create a custom GStreamer plugin that can monitor and analyze video frames. This is just the beginning - these same concepts will form the foundation for our AI video analytics system. Remember, understanding how data flows through your pipeline is crucial for building efficient multimedia applications.</p> <p>Stay tuned for the next article where we’ll start adding some real intelligence to our pipeline! 🚀</p>]]></content><author><name></name></author><category term="tutorials"/><category term="GStreamer,"/><category term="development,"/><category term="C++,"/><category term="buffers,"/><category term="plugins,"/><category term="video"/><category term="analytics"/><summary type="html"><![CDATA[Deep dive into GStreamer's core concepts as we begin building a real-time video analytics system.]]></summary></entry><entry><title type="html">Lattice-Based Threshold Signature Schemes</title><link href="https://rosemary-crypto.github.io/blog/2024/lattice-based-threshold-signature-schemes/" rel="alternate" type="text/html" title="Lattice-Based Threshold Signature Schemes"/><published>2024-09-22T15:12:00+00:00</published><updated>2024-09-22T15:12:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2024/lattice-based-threshold-signature-schemes</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2024/lattice-based-threshold-signature-schemes/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="introduction-to-lattice-based-threshold-signature-schemes">Introduction to Lattice-Based Threshold Signature Schemes</h2> <p>Imagine you’re working in a blockchain network, and you don’t want one person to have full control of the private key needed to sign transactions. That sounds risky, right? Well, this is where <strong>threshold signature schemes</strong> (TSS) come into play. Instead of one person holding the key, multiple participants each hold a piece of it. And here’s the twist: none of them can reconstruct the entire key on their own. But when they work together, they can still sign a valid transaction or document. I’ve written about them <a href="https://rosemary-crypto.github.io/blog/2024/introduction-to-threshold-signature-schemes/">here</a> if you’re interested in learning more about them.</p> <p>Now, let’s add <strong>lattice-based cryptography</strong> into the mix. Why? Because lattices, with their quantum-resilient properties, are a superhero of cryptography. Lattice-based cryptography is super secure and, unlike traditional methods, isn’t vulnerable to attacks from quantum computers.</p> <p>In this article, we’re going to break down how <strong>lattice-based threshold signatures</strong> work and how they could be used in real-world applications like blockchain.</p> <hr/> <h2 id="what-makes-lattice-based-cryptography-so-special">What makes Lattice-Based Cryptography so special?</h2> <p>Lattice-based cryptography is built on mathematical structures called <strong>lattices</strong>—essentially grids of points in space. What makes lattices special is that some problems related to them (the Shortest Vector Problem (SVP) and the Learning With Errors (LWE) problems)are incredibly hard to solve, even for quantum computers! This makes them a go-to choice for post-quantum cryptographic systems.</p> <p>In a lattice-based threshold signature scheme, the key (or secret) is hidden using these lattice-based problems. And just like in traditional TSS, no single participant can reveal the whole secret.</p> <hr/> <h2 id="how-does-lattice-based-tss-work">How Does Lattice-Based TSS Work?</h2> <p>Let’s break it down in simple terms:</p> <h3 id="distributed-key-generation-dkeygen">Distributed Key Generation (DKeyGen)</h3> <p>In the DKeyGen process, the secret key is split into <strong>t-out-of-n shares</strong>. So if you have a group of participants, you can decide that at least <em>t</em> of them need to cooperate to produce a signature. And don’t worry, the public key is still valid for verifying signatures—just like any other signature scheme.</p> <h3 id="share-refreshment-sharerefresh">Share Refreshment (ShareRefresh)</h3> <p>To keep things secure, we can periodically <strong>refresh</strong> the shares. That way, if someone tries to collect information over time, they’ll get stuck with useless, old data. It’s like giving your system a security refresh without changing the underlying secret!</p> <h3 id="distributed-signing-dsign">Distributed Signing (DSign)</h3> <p>Once the participants have their shares of the secret key, they can each sign parts of a message. When combined, these <strong>partial signatures</strong> form a complete, valid signature. The key advantage here is that no one participant can sign anything on their own, but together they can create a fully verifiable signature.</p> <hr/> <h2 id="where-can-we-use-lattice-based-tss">Where Can We Use Lattice-Based TSS?</h2> <p>Now that we have a basic understanding of how it works, let’s talk about real-world applications.</p> <h3 id="multisignature-wallets-in-blockchain">Multisignature Wallets in Blockchain</h3> <p>Imagine a multisignature wallet in a blockchain network. You don’t want a single person controlling the private key that authorizes transactions. Instead, a group of trusted people each holds a piece of the private key. To sign a transaction, they need to work together. This enhances security and prevents any one person from making unauthorized moves.</p> <p>Lattice-based cryptography makes this even more powerful because it protects against future threats from quantum computers. That’s something traditional cryptographic schemes can’t do.</p> <h3 id="zero-knowledge-proofs-and-privacy">Zero-Knowledge Proofs and Privacy</h3> <p>Lattice-based cryptography is also great for <strong>zero-knowledge proofs</strong> (ZKPs), where you can prove something is true without revealing the details. Combine that with threshold signatures, and you have a system that’s not only secure but also privacy-preserving. It’s a perfect match for privacy-focused blockchain applications.</p> <hr/> <h2 id="implementation">Implementation</h2> <p>If you’re curious to see how lattice-based threshold signatures work under the hood, I’ve created a complete <strong>Python implementation</strong> of these concepts, loosely based on the paper <em>Efficient Lattice-Based Threshold Signatures with Functional Interchangeability</em>.</p> <p>You can check out the implementation in this <a href="https://github.com/rosemary-crypto/lattice-based-tss">GitHub repository</a>. The code walks you through distributed key generation, share refreshment, and distributed signing, so you can easily experiment with it and see how it could work for your own blockchain projects!</p> <hr/> <h2 id="conclusion">Conclusion</h2> <p>Lattice-based threshold signature schemes offer a powerful and secure way to distribute trust in decentralized systems like blockchain. By splitting the private key across multiple participants and using lattice-based cryptography to enhance security, we can protect against both current and future threats, including quantum attacks.</p> <p>Whether you’re building a multisignature wallet, exploring zero-knowledge proofs, or simply interested in post-quantum cryptography, lattice-based TSS is an exciting area to dive into. And with the implementation linked in the GitHub repository, you can start exploring this cutting-edge cryptographic technique today!</p> <hr/> <h3 id="references">References</h3> <ol> <li>Tang et al., Efficient Lattice-Based Threshold Signatures with Functional Interchangeability. 2024.</li> </ol>]]></content><author><name></name></author><category term="cryptography"/><category term="cryptography,"/><category term="threshold"/><category term="signatures,"/><category term="lattice-based"/><category term="cryptography,"/><category term="blockchain"/><summary type="html"><![CDATA[An introduction to lattice-based threshold signatures, their applications, and a Python implementation.]]></summary></entry><entry><title type="html">Introduction to Threshold Signature Schemes</title><link href="https://rosemary-crypto.github.io/blog/2024/introduction-to-threshold-signature-schemes/" rel="alternate" type="text/html" title="Introduction to Threshold Signature Schemes"/><published>2024-09-19T15:12:00+00:00</published><updated>2024-09-19T15:12:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2024/introduction-to-threshold-signature-schemes</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2024/introduction-to-threshold-signature-schemes/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="introduction">Introduction</h2> <p>The idea of trusting a single person or system with <em>everything</em>—be it sensitive data, a secret, or even the keys to the kingdom—makes everyone a little nervous. What if they slip up? What if someone gets compromised? That’s where distributing the trust across multiple people or systems comes in handy.</p> <p>Enter <strong>Multiparty Computation (MPC)</strong> and <strong>Multiparty Key Management Systems (MKMS)</strong>. These cryptographic techniques allow multiple people to work together to perform tasks, like signing a document or approving a transaction, <em>without</em> any of them revealing their private information. It’s like having everyone contribute a piece of the puzzle, but no one person has all the pieces.</p> <p>We’re going to explore something called <strong>Threshold Signature Schemes (TSS)</strong>, a close relative of secret sharing (which we covered in a <a href="https://rosemary-crypto.github.io/blog/2024/introduction-to-secret-sharing/">previous post</a>), but with a twist: rather than simply protecting a secret, TSS lets a group of participants <em>collaborate</em> to generate a valid digital signature. And the coolest part? No one has the full private key.</p> <h2 id="what-is-tss">What is TSS?</h2> <p>So, what exactly is <strong>Threshold Signature Schemes (TSS)</strong>? Well, it’s all about splitting a private signing key into multiple pieces or shares, and only when a certain number of these shares (the threshold) come together can a valid signature be produced. Think of it like this: imagine you and your friends want to sign a really important document, but you don’t want just one person in charge of the pen. Instead, the pen is split into pieces, and only when enough pieces are put together can you write the signature. That’s TSS in a nutshell.</p> <p>Let’s take a practical example: you have a wallet in a decentralized exchange. Instead of one person holding the private key, TSS ensures that multiple parties hold <em>pieces</em> of that key. When it’s time to sign a transaction, a certain number of those people (the threshold) need to work together to create the signature.</p> <p>The beauty of TSS is that even though no one has the full private key, they can still collaborate to generate a valid signature. No single party holds too much power, but the system stays functional even if some shares are unavailable. It’s like teamwork, but for cryptography!</p> <h2 id="the-math-behind-tss-elliptic-curve-cryptography">The Math Behind TSS: Elliptic Curve Cryptography</h2> <p>Now we’re going to dive a bit deeper into the math behind TSS. This will help you understand how TSS works and why it’s secure.</p> <h3 id="what-is-elliptic-curve-cryptography-ecc">What is Elliptic Curve Cryptography (ECC)?</h3> <p><strong>Elliptic Curve Cryptography (ECC)</strong> is a type of public-key cryptography based on the algebraic structure of elliptic curves over finite fields. ECC allows for smaller keys compared to other cryptosystems like RSA while providing the same level of security. This efficiency makes ECC widely used in modern cryptography.</p> <p>An <strong>elliptic curve</strong> is defined by an equation of the form:</p> \[y^2 = x^3 + ax + b\] <p>where \(a\) and \(b\) are constants that satisfy the condition \(4a^3 + 27b^2 \neq 0\) to ensure that the curve has no singularities (i.e., it is smooth).</p> <p>In ECC, we work over a <strong>finite field</strong>, which means we only use integer coordinates within a certain range (modulo a prime number \(p\)). This ensures that there are a finite number of points on the curve, making computations feasible for cryptographic purposes.</p> <h3 id="basic-operations-on-elliptic-curves">Basic Operations on Elliptic Curves</h3> <p>The primary operation in ECC is <strong>point addition</strong>:</p> <ul> <li><strong>Point Addition (\(P + Q\))</strong>: Given two points \(P\) and \(Q\) on the curve, their sum \(R = P + Q\) is also a point on the curve, defined by specific algebraic formulas.</li> <li><strong>Point Doubling (\(2P\))</strong>: This is a special case of point addition where \(P = Q\).</li> </ul> <p>These operations are used to define <strong>scalar multiplication</strong>:</p> <ul> <li><strong>Scalar Multiplication (\(kP\))</strong>: This means adding the point \(P\) to itself \(k\) times. Scalar multiplication is computationally easy in one direction but hard to reverse, which is the basis for ECC’s security.</li> </ul> <h3 id="ecc-in-cryptography">ECC in Cryptography</h3> <p>In ECC-based cryptography, we have:</p> <ul> <li> <p><strong>Private Key (\(s\))</strong>: A randomly selected integer within a certain range.</p> </li> <li> <p><strong>Public Key (\(P\))</strong>: Calculated as \(P = sG\), where \(G\) is a predefined generator point on the elliptic curve.</p> </li> </ul> <p>The security of ECC comes from the <strong>Elliptic Curve Discrete Logarithm Problem (ECDLP)</strong>: Given \(P\) and \(G\), it’s computationally infeasible to determine \(s\).</p> <h3 id="how-tss-utilizes-ecc">How TSS Utilizes ECC</h3> <p>In Threshold Signature Schemes, ECC is used to allow multiple participants to collaborate on signing a message without revealing their individual private keys.</p> <h4 id="key-generation-in-tss-ecc-based">Key Generation in TSS (ECC-based)</h4> <ol> <li> <p><strong>Private Key Sharing</strong>:</p> <ul> <li>The original private key \(s\) is split into \(n\) shares \(s_1, s_2, \dots, s_n\) using a secret sharing scheme (like Shamir’s Secret Sharing).</li> <li>Each participant \(i\) holds a share \(s_i\).</li> </ul> </li> <li> <p><strong>Public Key Computation</strong>:</p> <ul> <li>The public key remains \(P = sG\).</li> <li>Each participant can compute their own public share \(P_i = s_i G\).</li> </ul> </li> </ol> <h4 id="signature-generation">Signature Generation</h4> <p>When a message \(m\) needs to be signed:</p> <ol> <li> <p><strong>Partial Signatures</strong>:</p> <ul> <li>Each participant computes a partial signature \(\sigma_i\) using their private share \(s_i\).</li> <li>The signature process involves generating a random nonce \(k_i\) and computing \(R_i = k_i G\).</li> </ul> </li> <li> <p><strong>Combining Partial Signatures</strong>:</p> <ul> <li>The participants broadcast their \(R_i\) values and use them to compute a combined \(R\).</li> <li>They then use their \(s_i\) and \(k_i\) to compute their partial signatures \(\sigma_i\).</li> </ul> </li> <li> <p><strong>Final Signature</strong>:</p> <ul> <li>The partial signatures \(\sigma_i\) are combined using Lagrange interpolation to produce the final signature \(\sigma\).</li> </ul> </li> </ol> <h4 id="signature-verification">Signature Verification</h4> <p>The final signature \(\sigma\) can be verified using the public key \(P\) and the combined \(R\):</p> \[sG = R + eP\] <p>where \(e\) is the hash of the message and \(R\).</p> <h3 id="why-ecc-is-suitable-for-tss">Why ECC is Suitable for TSS</h3> <ul> <li> <p><strong>Efficiency</strong>: ECC allows for smaller keys and faster computations compared to other cryptographic systems.</p> </li> <li> <p><strong>Security</strong>: The hardness of the ECDLP ensures that even if an attacker obtains partial information, they cannot reconstruct the private key.</p> </li> <li> <p><strong>Scalability</strong>: ECC-based TSS can handle a large number of participants without significant performance degradation.</p> </li> </ul> <h3 id="want-to-see-ecc-in-action">Want to See ECC in Action?</h3> <p>If you’re interested in seeing a practical implementation of ECC and how it integrates with TSS, check out this <a href="https://github.com/rosemary-crypto/ECC-TSS-Demo">GitHub repository</a>. There, you’ll find code examples that walk you through:</p> <ul> <li>Generating elliptic curves and keys.</li> <li>Performing point addition and scalar multiplication.</li> <li>Implementing TSS using ECC, including key sharing, partial signature generation, and signature verification.</li> </ul> <p>By exploring the code, you’ll gain a deeper understanding of how ECC works under the hood and how it powers Threshold Signature Schemes.</p> <h2 id="choosing-the-right-tss-protocol">Choosing the Right TSS Protocol</h2> <p>There’s no one-size-fits-all TSS protocol. Depending on what you’re trying to do, different variations of TSS might be more suitable. Here are a few popular ones:</p> <ol> <li> <p><strong>Shamir-based TSS</strong>:</p> <ul> <li><strong>Strengths</strong>: Simple, easy to implement, and mathematically elegant.</li> <li><strong>Weaknesses</strong>: If all shares are gathered, the private key can be reconstructed, which might pose a security risk.</li> </ul> </li> <li> <p><strong>Pedersen’s TSS</strong>:</p> <ul> <li><strong>Strengths</strong>: Uses verifiable secret sharing to ensure that malicious participants can’t provide false shares.</li> <li><strong>Weaknesses</strong>: A bit more computationally intensive, but worth it if you need the added security.</li> </ul> </li> <li> <p><strong>Feldman’s TSS</strong>:</p> <ul> <li><strong>Strengths</strong>: Adds public verification, so you can easily check if shares are valid without revealing anything sensitive.</li> <li><strong>Weaknesses</strong>: Like Pedersen’s, it’s more computationally demanding than Shamir’s, but it’s also more secure.</li> </ul> </li> </ol> <p>Each protocol comes with its own balance of security and computational complexity, so the right one depends on your specific needs.</p> <h2 id="real-world-applications-of-tss">Real-World Applications of TSS</h2> <p>Now that you have a grasp on how TSS works, let’s talk about where it’s being used today.</p> <h3 id="1-blockchain-and-decentralized-finance-defi">1. Blockchain and Decentralized Finance (DeFi)</h3> <p>In platforms like Bitcoin and Ethereum, TSS is used to manage multi-signature wallets. With TSS, multiple parties must approve a transaction before it can be executed, making it a perfect solution for secure fund management.</p> <h3 id="2-secure-voting-systems">2. Secure Voting Systems</h3> <p>In some e-voting systems, TSS allows a group of voters to securely sign off on a decision without revealing individual votes. It’s a great way to ensure that everyone has a say without compromising privacy.</p> <h3 id="3-distributed-key-management">3. Distributed Key Management</h3> <p>In cloud environments, TSS allows multiple servers or entities to manage cryptographic keys without any one server having the full key. This prevents key exposure and enhances overall security.</p> <h3 id="4-privacy-preserving-protocols">4. Privacy-Preserving Protocols</h3> <p>TSS is also crucial for privacy-preserving systems, like secure multiparty computation (SMPC), where parties work together to compute a function without revealing their individual inputs.</p> <h2 id="security-and-efficiency-what-to-consider">Security and Efficiency: What to Consider</h2> <p>TSS gives you a ton of security advantages, but as always, there are trade-offs to keep in mind:</p> <h3 id="security-advantages">Security Advantages:</h3> <ul> <li><strong>No Single Point of Failure</strong>: Even if some participants are compromised, the system stays secure unless the threshold is met.</li> <li><strong>Distributed Trust</strong>: TSS distributes control among multiple parties, reducing the risk of any one person holding too much power.</li> <li><strong>Collusion Resistance</strong>: With verifiable secret sharing, participants can’t lie about their shares without being caught.</li> </ul> <h3 id="efficiency-considerations">Efficiency Considerations:</h3> <ul> <li><strong>Computational Overhead</strong>: Generating and combining signatures is more computationally expensive than traditional methods.</li> <li><strong>Communication Overhead</strong>: Participants need to communicate to share their pieces, which can add some delay, especially in large systems.</li> </ul> <p>Balancing security and efficiency is key when deciding how to implement TSS in a large-scale system.</p> <h2 id="conclusion">Conclusion</h2> <p>So there you have it! <strong>Threshold Signature Schemes (TSS)</strong> are an incredible tool in the cryptography world, offering the best of both security and distributed trust. Whether you’re building a decentralized finance platform, a secure voting system, or managing cryptographic keys in the cloud, TSS can help protect your system from single points of failure and enhance overall security.</p> <p>Remember, understanding the math behind these concepts can deepen your grasp of how they work and why they’re secure. If you want to explore ECC and TSS in more detail, including practical code examples, feel free to check out the <a href="https://github.com/rosemary-crypto/ECC-TSS-Demo">GitHub repository</a>. It’s a great resource to see these principles in action.</p> <p>Happy coding and stay secure!</p>]]></content><author><name></name></author><category term="cryptography"/><category term="cryptography,"/><category term="TSS,"/><category term="distributed"/><category term="systems"/><summary type="html"><![CDATA[An introduction to Threshold Signature Schemes and their practical applications]]></summary></entry><entry><title type="html">Introduction to Secret Sharing</title><link href="https://rosemary-crypto.github.io/blog/2024/introduction-to-secret-sharing/" rel="alternate" type="text/html" title="Introduction to Secret Sharing"/><published>2024-08-20T15:12:00+00:00</published><updated>2024-08-20T15:12:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2024/introduction-to-secret-sharing</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2024/introduction-to-secret-sharing/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="secret-sharing">Secret Sharing</h2> <p>I wouldn’t call myself a secretive person, but I do value my privacy. If you’re like me, you’ve probably found yourself holding onto a secret so tightly that you start feeling like it might just burst out. But instead of spilling the whole thing to one person, you decide to share bits and pieces with different people. This way, no single person knows the full story, and you feel a bit more secure.</p> <p>Secret sharing works in a similar way. Introduced by Adi Shamir and George Blakley independently in 1979 (yes, it’s been around for a while, but like a good classic, it’s still relevant), secret sharing is all about distributing a secret among a group of participants. The twist? No single participant has any meaningful information on their own. However, when a sufficient number of these participants come together, they can reconstruct the original secret.</p> <p>In this post, we’ll explore Shamir’s version of secret sharing, a powerful cryptographic tool that splits a secret into pieces, or “shares.” These shares are individually useless, but when you have enough of them (we’ll call this number the threshold), you can piece together the original secret.</p> <h2 id="mathematical-definition-of-shamirs-secret-sharing">Mathematical Definition of Shamir’s Secret Sharing</h2> <p>Let’s get a bit more technical. SSS is sometimes referred to as the \((k, n)\) threshold secret sharing scheme. Here, \(n\) is the number of shares into which the secret is divided, and \(k\) is the minimum number of shares required to reconstruct the secret. Essentially, \(k\) participants must come together to unlock the secret. If you have fewer than \(k\) shares, you’re out of luck; the secret remains hidden.</p> <p>But how does this actually work? The secret is hidden inside a mathematical function called a polynomial. A polynomial is just a mathematical expression involving a sum of powers of \(x\) multiplied by coefficients. In Shamir’s scheme, we use a polynomial of degree \(k-1\), which ensures that any group of \(k\) or more participants can reconstruct the secret.</p> \[f(x) = s_0 + s_1x + \dots + s_{k-1}x^{k-1} \mod p\] <p>Lagrange’s interpolation can be used to reconstruct the secret. The Lagrange interpolation formula for reconstructing the polynomial (and thus the secret) from these shares is:</p> \[q(x) = \sum_{i=1}^{k} y_i \cdot L_i(x)\] <p>where \(y_i\) are the share values, and \(L_i(x)\) are the Lagrange basis polynomials, defined as:</p> \[L_i(x) = \prod_{\substack{1 \le j \le k \\ j \ne i}} \frac{x - x_j}{x_i - x_j}\] <h2 id="why-modular-arithmetic-and-prime-numbers">Why Modular Arithmetic and Prime Numbers?</h2> <ul> <li><strong>Modular arithmetic</strong> is a system of arithmetic for integers, where numbers wrap around after they reach a certain value, \(p\) (the modulus). Think of it like a clock: after 12 comes 1 again. In cryptography, modular arithmetic helps keep numbers within a specific range, making it more manageable and secure.</li> <li><strong>Prime numbers</strong> are crucial because they ensure certain properties that are vital for cryptographic security: <ul> <li><strong>Unique Inverses</strong>: In modular arithmetic with a prime modulus \(p\), every non-zero number has a unique multiplicative inverse. This means that for any non-zero number \(a\) within the range \([1, p-1]\), there exists a number \(b\) such that \((a \cdot b) \mod p = 1\). This property is crucial because it allows division operations to be performed in modular arithmetic.</li> <li><strong>Avoiding Redundancies</strong>: Prime moduli help avoid redundancies and ensure that every possible result of an operation is unique within the range \([0, p-1]\). This uniqueness is critical in cryptography because it prevents different numbers from being treated as equivalent, which could otherwise lead to vulnerabilities.</li> <li><strong>Mathematical Stability</strong>: Using a prime number ensures that the structure of the arithmetic remains stable, making it easier to predict and manage the behavior of calculations, which is why primes are often used in cryptographic protocols.</li> </ul> </li> </ul> <h2 id="basic-example-with-numbers">Basic Example with Numbers</h2> <p>Let’s start simple. Imagine you have a secret number, and you want to split it among five people so that any three of them can come together and recover it. You’ll create a polynomial, and each person gets a point on this polynomial as their share. Only when three or more people pool their points together can they figure out what the original number (the secret) was.</p> <h3 id="generating-the-shares">Generating the Shares</h3> <p>Let’s take a \((2, 5)\) threshold scheme to divide the secret 298 and let the prime number be 307. (I chose 307 since it is a prime number larger than 298)</p> <ol> <li> <p><strong>Set up the polynomial</strong></p> <ul> <li>Degree: \(k-1 = 1\)</li> <li>Form: \(q(x) = s + a_1 \cdot x \mod p = 298 + a_1 \cdot x \mod 307\)</li> </ul> </li> <li> <p><strong>Choose a random coefficient</strong> \(a_1\) in the range \([0, p)\). Let’s choose \(a_1 = 123\).</p> </li> <li> <p><strong>Calculate the shares</strong>: Now, we can calculate the shares at 5 non-zero points \(x = 1, 2, 3, 4, 5\) as:</p> <ul> <li>Share 1: \((1, 114)\)</li> <li>Share 2: \((2, 237)\)</li> <li>Share 3: \((3, 53)\)</li> <li>Share 4: \((4, 176)\)</li> <li>Share 5: \((5, 299)\)</li> </ul> </li> </ol> <p>You now have 5 shares: \((1, 114), (2, 237), (3, 53), (4, 176), (5, 299)\).</p> <h3 id="reconstructing-the-secret-using-lagrange-interpolation">Reconstructing the Secret Using Lagrange Interpolation</h3> <p>Let’s now reconstruct the secret using Lagrange interpolation. Since this is a \((2, 5)\) threshold scheme, we need only two of the shares to reconstruct the secret. We’ll use Share 1 \((1, 114)\) and Share 2 \((2, 237)\).</p> <ol> <li> <p><strong>Calculate the Lagrange basis</strong></p> <ul> <li>For our example, with \(x_1 = 1\) and \(x_2 = 2\), the Lagrange basis polynomials at \(x = 0\) (which gives us the secret) are:</li> </ul> \[L_1(0) = \frac{0 - 2}{1 - 2} = \frac{-2}{-1} = 2\] \[L_2(0) = \frac{0 - 1}{2 - 1} = \frac{-1}{1} = -1\] </li> <li> <p><strong>Calculate the Secret</strong></p> <ul> <li>Now, we use these Lagrange basis polynomials to reconstruct the secret:</li> </ul> \[q(0) = y_1 \cdot L_1(0) + y_2 \cdot L_2(0)\] <ul> <li>Substituting the values from the shares:</li> </ul> \[q(0) = 114 \cdot 2 + 237 \cdot (-1) = 228 - 237 = -9\] <ul> <li>Since we’re working in modular arithmetic with \(p = 307\), we take the result modulo 307:</li> </ul> \[q(0) = -9 \mod 307 = 298\] </li> </ol> <p>Thus, the reconstructed secret is \(s = 298\), which matches our original secret.</p> <h2 id="shamirs-secret-sharing-using-a-secret-image">Shamir’s Secret Sharing Using a Secret Image</h2> <p>To really bring this to life, let’s take it a step further. Instead of just splitting a number, let’s split an image. Lets take an example of (3,5) threshold secret sharing as shown in the figure.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/secret_sharing/3_5_SecretImageSharing-480.webp 480w,/assets/img/secret_sharing/3_5_SecretImageSharing-800.webp 800w,/assets/img/secret_sharing/3_5_SecretImageSharing-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/secret_sharing/3_5_SecretImageSharing.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> ((3,5) Threshold Secret Image Sharing) </div> <h3 id="how-it-works">How It Works</h3> <ul> <li> <p><strong>Image Splitting</strong>: Each pixel’s grayscale value in the image is treated as a secret. Shamir’s Secret Sharing algorithm is applied to split each pixel’s value into multiple shares. For instance, in a \((3,5)\) threshold scheme, the image is divided into 5 shares, but at least 3 shares are needed to reconstruct the image.</p> </li> <li> <p><strong>Share Generation</strong>: For each pixel, shares are generated based on a random polynomial where the pixel value is the constant term. These shares are then distributed, with each share forming part of a different image, each appearing as random noise.</p> </li> <li> <p><strong>Image Reconstruction</strong>: To reconstruct the image, a minimum of 3 shares are combined using Lagrange interpolation to recover the original pixel values and, thus, the entire image.</p> </li> </ul> <h3 id="code-implementation">Code Implementation</h3> <p>The complete implementation of this concept is provided in the repository. The code is available in both C++ and Rust:</p> <ul> <li> <p><strong>C++ Version</strong>:</p> <ul> <li>Located in the <code class="language-plaintext highlighter-rouge">cpp</code> directory, this version uses OpenCV for image processing.</li> </ul> </li> <li> <p><strong>Rust Version</strong>:</p> <ul> <li>Located in the <code class="language-plaintext highlighter-rouge">rust</code> directory.</li> </ul> </li> </ul> <p>The code is accessible on <a href="https://github.com/rosemary-crypto/Shamir-sSecretSharing">GitHub</a> for further exploration and application.</p> <h2 id="significance-in-modern-applications">Significance in Modern Applications</h2> <p>Now, why should you care about this old-school cryptography trick? Well, let’s dive into the world of Threshold Signature Schemes (TSS). Imagine you’re working on a distributed system, like a blockchain. In such systems, it’s crucial that a group of participants can collaboratively sign off on transactions or documents. But here’s the catch: you don’t want any single participant holding the entire signing key, because that’s a single point of failure and a potential security nightmare.</p> <p>TSS comes to the rescue by allowing a group of participants to generate a digital signature on a message without anyone holding the full key. Instead, the key is distributed, and only a subset of participants—what we call the threshold number—needs to come together to produce a valid signature. This method not only enhances security by eliminating single points of failure but also builds trust, as no single party controls the entire signing process.</p> <p>However, TSS is more complex than SSS, but that’s why we’re here. Before diving into TSS, it’s important to understand SSS and its limitations so we can appreciate the need for more advanced techniques like TSS.</p>]]></content><author><name></name></author><category term="cryptography"/><category term="cryptography,"/><category term="secret"/><category term="sharing,"/><category term="TSS"/><summary type="html"><![CDATA[An introduction to Shamir's Secret Sharing and its applications]]></summary></entry><entry><title type="html">Getting Started with GStreamer Development – A Beginner’s Guide</title><link href="https://rosemary-crypto.github.io/blog/2024/build-with-gstreamer-Lesson-1/" rel="alternate" type="text/html" title="Getting Started with GStreamer Development – A Beginner’s Guide"/><published>2024-08-14T23:00:00+00:00</published><updated>2024-08-14T23:00:00+00:00</updated><id>https://rosemary-crypto.github.io/blog/2024/build-with-gstreamer-Lesson-1</id><content type="html" xml:base="https://rosemary-crypto.github.io/blog/2024/build-with-gstreamer-Lesson-1/"><![CDATA[<style>.post-content h2{font-size:2rem;font-weight:600;color:#00543d;border-bottom:2px solid #00543d;padding-bottom:.5rem;margin-top:2rem;margin-bottom:1rem}.post-content h3{font-size:1.75rem;font-weight:500;color:#264653;margin-top:1.5rem;margin-bottom:.75rem}.post-content h4{font-size:1rem;font-weight:500;color:#e76f51;margin-top:1.25rem;margin-bottom:.5rem;margin-left:1.5rem}.post-content h4+*{margin-left:1.5rem}</style> <h2 id="introduction">Introduction</h2> <p>Around a year and a half ago, I found myself tasked with rebuilding an AI video analytics platform. The goal? To transition it to use the GStreamer framework. At first, it seemed like an insurmountable challenge. I spent countless nights grappling with GStreamer’s intricacies—confused by the difference between a sink and a source, haunted by the complexities of GStreamer buffers. But as I pushed through, I started to see the beauty in it. I can’t believe I’m saying this, but GStreamer has made me fall deeper in love with C and C++.</p> <p>In this guide, I’ll introduce you to the basics of GStreamer, sharing what I’ve learned along the way to help you get started with building your own multimedia applications. Let’s dive in!</p> <h3 id="what-is-gstreamer">What is GStreamer?</h3> <p>GStreamer is a powerful multimedia framework used for building media processing pipelines. Whether you’re dealing with video, audio, or any other type of media, GStreamer provides the tools you need to manipulate and process data efficiently.</p> <p>At its core, GStreamer is built around the concept of pipelines—sequences of elements that process data from one end to another. These elements can be sources (e.g., reading from a file), filters (e.g., converting formats), or sinks (e.g., outputting to a display or speaker). Think of it as a factory assembly line, where raw materials (your media) are processed and refined at various stations (the elements), eventually leading to a finished product (your output).</p> <h3 id="what-am-i-using-gstreamer-for">What Am I Using GStreamer For?</h3> <p>For my AI video analytics platform, GStreamer acts as the backbone. The simplest version of the process is relatively straightforward: reading from a source (whether that’s video files, RTSP streams, etc.), performing object detection, tracking, and finally displaying the results. However, the full platform involves much more. It includes decoding the video streams, pre-processing the data, performing advanced object detection and tracking, video classification, adding custom metadata, sending data over MQTT, streaming via RTSP, and even detecting specific events. This complex pipeline allows for real-time analytics and decision-making, making GStreamer an essential tool in building this robust and scalable solution.</p> <p>You might wonder, “Why not just use something like NVIDIA’s DeepStream SDK?” It’s a valid question, and honestly, DeepStream could simplify things. But where’s the fun in that? GStreamer is open-source, allowing me to tweak and modify the code as needed. When existing plugins don’t meet my requirements, I have the freedom to build my own. This series is about giving you that same power: developing your own custom GStreamer elements for Linux, Windows, and macOS. So, let’s continue with the basics.</p> <h2 id="the-set-up">The Set-Up</h2> <h3 id="how-do-you-start">How Do You Start?</h3> <p>There’s no better place to understand GStreamer concepts than by exploring the <a href="https://gstreamer.freedesktop.org/documentation/application-development/?gi-language=c">GStreamer Application Development Manual</a>. It’s a treasure trove of information, and while I won’t repeat the concepts here, I’ll guide you through navigating everything, making your learning process smoother.</p> <h3 id="installing-gstreamer">Installing GStreamer</h3> <p>Before diving into examples, you’ll need to install GStreamer on your system. I suggest following the installation instructions in the <a href="https://gstreamer.freedesktop.org/documentation/installing/index.html?gi-language=c">GStreamer Application Development Manual</a> for your specific environment, whether that’s Linux, Windows, or macOS.</p> <h2 id="creating-a-simple-pipeline">Creating a Simple Pipeline</h2> <p>To understand GStreamer, it’s crucial to get hands-on experience. Let’s start by creating a simple pipeline to display a video.</p> <h3 id="pipeline-diagram">Pipeline Diagram</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/build_with_gstreamer/basic-pipeline-lesson-1-480.webp 480w,/assets/img/build_with_gstreamer/basic-pipeline-lesson-1-800.webp 800w,/assets/img/build_with_gstreamer/basic-pipeline-lesson-1-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/build_with_gstreamer/basic-pipeline-lesson-1.png" class="img-fluid rounded z-depth-1" width="70%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>This diagram shows a simple pipeline with elements like <code class="language-plaintext highlighter-rouge">filesrc</code>, <code class="language-plaintext highlighter-rouge">decodebin</code>, and <code class="language-plaintext highlighter-rouge">autovideosink</code> connected together.</p> <h3 id="understanding-the-basics">Understanding the Basics</h3> <ul> <li><strong>Elements:</strong> These are the basic building blocks of a GStreamer pipeline. Each element has a specific role, such as reading data (source), processing it (filter), or outputting it (sink). For example: <ul> <li><code class="language-plaintext highlighter-rouge">filesrc</code>: Reads data from a file.</li> <li><code class="language-plaintext highlighter-rouge">decodebin</code>: Automatically detects and decodes the data.</li> <li><code class="language-plaintext highlighter-rouge">autovideosink</code>: Displays the video on your screen.</li> </ul> </li> <li> <p><strong>Pads:</strong> A pad is a point of connection in a GStreamer element, where media data flows in or out. Every element has two types of pads: source pads (where data exits the element) and sink pads (where data enters). This concept mirrors real-world scenarios—a water faucet (source) provides water, while a drain (sink) receives it.</p> </li> <li> <p><strong>Bins:</strong> A bin is a container for elements. It groups multiple elements together, managing them as a single entity. This is particularly useful when dealing with complex pipelines. For example, <code class="language-plaintext highlighter-rouge">decodebin</code> is a bin because it automatically detects the format of incoming data, creates the necessary elements to decode it, and manages them internally. You don’t have to worry about what’s inside; it’s like a black box that simplifies your pipeline design.</p> </li> <li><strong>Pipelines:</strong> A pipeline is a special type of bin that manages the flow of data between elements. It controls the state of the elements and ensures data flows correctly from source to sink. Pipelines can contain multiple elements and bins, working together to process and deliver media.</li> </ul> <h3 id="command-line">Command Line</h3> <p>Let’s create the pipeline shown in the diagram using GStreamer’s command-line tool:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>/path/to/video.mp4 <span class="o">!</span> decodebin <span class="o">!</span> autovideosink
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">filesrc location=/path/to/video.mp4</code>: This element reads the video file.</li> <li><code class="language-plaintext highlighter-rouge">decodebin</code>: Automatically detects and decodes the video format.</li> <li><code class="language-plaintext highlighter-rouge">autovideosink</code>: Displays the video on your screen.</li> <li><code class="language-plaintext highlighter-rouge">gst-launch-1.0</code>: A command-line tool provided by GStreamer to run pipelines.</li> <li><code class="language-plaintext highlighter-rouge">!</code>: Links elements together within the pipeline.</li> </ul> <h3 id="writing-the-equivalent-gstreamer-application-in-c">Writing the Equivalent GStreamer Application in C++</h3> <p>While using the command line is great for quick tests, writing your own applications using GStreamer’s API is where the real fun begins. Below is the part of the code to create a basic video player in C++:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span><span class="p">,</span> <span class="kt">char</span><span class="o">*</span> <span class="n">argv</span><span class="p">[])</span> <span class="p">{</span>
    <span class="n">GMainLoop</span><span class="o">*</span> <span class="n">loop</span><span class="p">;</span>
    <span class="n">GstBus</span><span class="o">*</span> <span class="n">bus</span><span class="p">;</span>
    <span class="n">guint</span> <span class="n">bus_watch_id</span><span class="p">;</span>

    <span class="c1">// Initialize GStreamer</span>
    <span class="n">gst_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">argc</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">argv</span><span class="p">);</span>

    <span class="c1">// Create a main loop, which will run until we explicitly quit</span>
    <span class="n">loop</span> <span class="o">=</span> <span class="n">g_main_loop_new</span><span class="p">(</span><span class="nb">NULL</span><span class="p">,</span> <span class="n">FALSE</span><span class="p">);</span>

    <span class="c1">// Create the elements</span>
    <span class="n">GstElement</span><span class="o">*</span> <span class="n">pipeline</span> <span class="o">=</span> <span class="n">gst_pipeline_new</span><span class="p">(</span><span class="s">"video-player"</span><span class="p">);</span>
    <span class="n">GstElement</span><span class="o">*</span> <span class="n">source</span> <span class="o">=</span> <span class="n">gst_element_factory_make</span><span class="p">(</span><span class="s">"filesrc"</span><span class="p">,</span> <span class="s">"file-source1"</span><span class="p">);</span>
    <span class="n">GstElement</span><span class="o">*</span> <span class="n">decoder</span> <span class="o">=</span> <span class="n">gst_element_factory_make</span><span class="p">(</span><span class="s">"decodebin"</span><span class="p">,</span> <span class="s">"decode-bin1"</span><span class="p">);</span>
    <span class="n">GstElement</span><span class="o">*</span> <span class="n">autovideosink</span> <span class="o">=</span> <span class="n">gst_element_factory_make</span><span class="p">(</span><span class="s">"autovideosink"</span><span class="p">,</span> <span class="s">"video-output1"</span><span class="p">);</span>

    <span class="c1">// Check that all elements were created successfully</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">pipeline</span> <span class="o">||</span> <span class="o">!</span><span class="n">source</span> <span class="o">||</span> <span class="o">!</span><span class="n">decoder</span> <span class="o">||</span> <span class="o">!</span><span class="n">autovideosink</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">g_printerr</span><span class="p">(</span><span class="s">"Not all elements could be created.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
        <span class="k">return</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="c1">// Set the source file location</span>
    <span class="n">g_object_set</span><span class="p">(</span><span class="n">G_OBJECT</span><span class="p">(</span><span class="n">source</span><span class="p">),</span> <span class="s">"location"</span><span class="p">,</span> <span class="s">"../resources/videos/big_buck_bunny_scene.mp4"</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">);</span>

    <span class="c1">// Add all elements to the pipeline</span>
    <span class="n">gst_bin_add_many</span><span class="p">(</span><span class="n">GST_BIN</span><span class="p">(</span><span class="n">pipeline</span><span class="p">),</span> <span class="n">source</span><span class="p">,</span> <span class="n">decoder</span><span class="p">,</span> <span class="n">autovideosink</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">);</span>

    <span class="c1">// Link the source to the decoder (decoder will dynamically link to sink)</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">gst_element_link</span><span class="p">(</span><span class="n">source</span><span class="p">,</span> <span class="n">decoder</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">g_printerr</span><span class="p">(</span><span class="s">"Source and decoder could not be linked.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
        <span class="n">gst_object_unref</span><span class="p">(</span><span class="n">pipeline</span><span class="p">);</span>
        <span class="k">return</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="c1">// Connect the pad-added signal for the decoder</span>
    <span class="n">g_signal_connect</span><span class="p">(</span><span class="n">decoder</span><span class="p">,</span> <span class="s">"pad-added"</span><span class="p">,</span> <span class="n">G_CALLBACK</span><span class="p">(</span><span class="n">on_pad_added</span><span class="p">),</span> <span class="n">autovideosink</span><span class="p">);</span>

    <span class="c1">// Set the pipeline to the playing state</span>
    <span class="n">gst_element_set_state</span><span class="p">(</span><span class="n">pipeline</span><span class="p">,</span> <span class="n">GST_STATE_PLAYING</span><span class="p">);</span>

    <span class="c1">// Start the main loop and wait until it is quit (e.g., via EOS or an error)</span>
    <span class="n">g_main_loop_run</span><span class="p">(</span><span class="n">loop</span><span class="p">);</span>

    <span class="c1">// Clean up: set the pipeline to NULL state, remove bus watch, and unref</span>
    <span class="n">gst_element_set_state</span><span class="p">(</span><span class="n">pipeline</span><span class="p">,</span> <span class="n">GST_STATE_NULL</span><span class="p">);</span>
    <span class="n">gst_object_unref</span><span class="p">(</span><span class="n">GST_OBJECT</span><span class="p">(</span><span class="n">pipeline</span><span class="p">));</span>
    <span class="n">g_main_loop_unref</span><span class="p">(</span><span class="n">loop</span><span class="p">);</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// Callback function to dynamically link pads</span>
<span class="kt">void</span> <span class="nf">on_pad_added</span><span class="p">(</span><span class="n">GstElement</span><span class="o">*</span> <span class="n">src</span><span class="p">,</span> <span class="n">GstPad</span><span class="o">*</span> <span class="n">new_pad</span><span class="p">,</span> <span class="n">GstElement</span><span class="o">*</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">GstPad</span><span class="o">*</span> <span class="n">sink_pad</span> <span class="o">=</span> <span class="n">gst_element_get_static_pad</span><span class="p">(</span><span class="n">sink</span><span class="p">,</span> <span class="s">"sink"</span><span class="p">);</span>

    <span class="c1">// Check if the sink pad is already linked</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">gst_pad_is_linked</span><span class="p">(</span><span class="n">sink_pad</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">g_object_unref</span><span class="p">(</span><span class="n">sink_pad</span><span class="p">);</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="c1">// Attempt to link the newly created pad with the sink pad</span>
    <span class="n">GstPadLinkReturn</span> <span class="n">ret</span> <span class="o">=</span> <span class="n">gst_pad_link</span><span class="p">(</span><span class="n">new_pad</span><span class="p">,</span> <span class="n">sink_pad</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">GST_PAD_LINK_FAILED</span><span class="p">(</span><span class="n">ret</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">g_printerr</span><span class="p">(</span><span class="s">"Type is '%s' but link failed.</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
            <span class="n">gst_structure_get_name</span><span class="p">(</span><span class="n">gst_caps_get_structure</span><span class="p">(</span><span class="n">gst_pad_get_current_caps</span><span class="p">(</span><span class="n">new_pad</span><span class="p">),</span> <span class="mi">0</span><span class="p">)));</span>
    <span class="p">}</span>

    <span class="n">g_object_unref</span><span class="p">(</span><span class="n">sink_pad</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">gst_init</code>: Initializes GStreamer and parses any command-line arguments.</li> <li><code class="language-plaintext highlighter-rouge">gst_element_factory_make</code>: Creates the elements used in the pipeline, such as the source, decoder, and video sink.</li> <li><code class="language-plaintext highlighter-rouge">gst_bin_add_many</code>: Adds all elements to the pipeline.</li> <li><code class="language-plaintext highlighter-rouge">gst_element_link</code>: Links the source element to the decoder. The decoder will dynamically link to the video sink when it determines the correct format.</li> <li><code class="language-plaintext highlighter-rouge">g_signal_connect</code>: Connects the “pad-added” signal for the decoder, allowing the new pad to be linked to the sink dynamically.</li> <li><code class="language-plaintext highlighter-rouge">gst_element_set_state</code>: Sets the pipeline’s state to PLAYING to start processing and displaying the video.</li> <li><code class="language-plaintext highlighter-rouge">g_main_loop_run</code>: Runs the main loop, keeping the application alive until an error occurs or the video finishes.</li> <li><code class="language-plaintext highlighter-rouge">gst_element_set_state</code>: Sets the pipeline’s state to NULL, stopping all elements when finished.</li> <li><code class="language-plaintext highlighter-rouge">gst_object_unref</code>: Frees up resources when the application is done.</li> </ul> <h4 id="understanding-the-on_pad_added-function">Understanding the <code class="language-plaintext highlighter-rouge">on_pad_added</code> Function</h4> <p>In GStreamer, some elements, like <code class="language-plaintext highlighter-rouge">decodebin</code>, dynamically create pads during runtime based on the media stream they are processing. They can’t link all their pads at the beginning because they don’t know what streams they will encounter (e.g., a video file might contain both audio and video streams).<br/> The <code class="language-plaintext highlighter-rouge">on_pad_added</code> function is a callback that gets triggered when the <code class="language-plaintext highlighter-rouge">decodebin</code> element adds a new pad. This is crucial because you need to link this newly created pad to the next element in your pipeline (in this case, <code class="language-plaintext highlighter-rouge">autovideosink</code>), which handles the video output.</p> <p>The complete code and detailed explanations, including installation instructions and how to run the program, are available on <a href="https://github.com/rosemary-crypto/build-with-gstreamer">GitHub</a>. This repository will be updated with each article in the series, making it easier for you to follow along.</p> <h3 id="github-repository-and-following-along">GitHub Repository and Following Along</h3> <p>To make the learning process even more interactive, I’ve created a <a href="https://github.com/rosemary-crypto/build-with-gstreamer">GitHub repository</a> where you can find all the code examples discussed in this series. Each module in this tutorial will be represented by a separate branch in the repository, allowing you to check out the specific branch to follow along with that module. As the course progresses, the <code class="language-plaintext highlighter-rouge">master</code> branch will be updated with the latest version of the project.</p> <ul> <li><strong>Start with Module 1:</strong> Check out the branch <code class="language-plaintext highlighter-rouge">lesson-1-getting-started</code> to see the code and examples for this introductory lesson.</li> <li><strong>Stay Updated:</strong> As you progress through the series, switch to the corresponding branch for each module to get the most relevant code and updates.</li> <li><strong>Explore the Code:</strong> Feel free to explore, modify, and experiment with the code in each branch to reinforce your learning.</li> </ul> <h3 id="compiling-and-running">Compiling and Running</h3> <p>Save the code to a file named <code class="language-plaintext highlighter-rouge">simple_player.cpp</code>. Then, compile it using <code class="language-plaintext highlighter-rouge">gcc</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gcc simple_player.cpp <span class="nt">-o</span> simple_player <span class="si">$(</span>pkg-config <span class="nt">--cflags</span> <span class="nt">--libs</span> gstreamer-1.0<span class="si">)</span>
</code></pre></div></div> <p>Finally, run the compiled program:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./simple_player
</code></pre></div></div> <h2 id="time-to-experiment">Time to Experiment!</h2> <p>Here are some fun experiments you can try to deepen your understanding of GStreamer pipelines:</p> <h3 id="1-play-with-different-sources">1. Play with Different Sources</h3> <p>Try modifying our basic pipeline to work with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Webcam input</span>
gst-launch-1.0 v4l2src <span class="o">!</span> videoconvert <span class="o">!</span> autovideosink

<span class="c"># Screen capture</span>
gst-launch-1.0 ximagesrc <span class="o">!</span> videoconvert <span class="o">!</span> autovideosink

<span class="c"># RTSP stream (replace with an actual RTSP URL)</span>
gst-launch-1.0 rtspsrc <span class="nv">location</span><span class="o">=</span>rtsp://example.com/stream <span class="o">!</span> decodebin <span class="o">!</span> videoconvert <span class="o">!</span> autovideosink
</code></pre></div></div> <h3 id="2-add-some-effects">2. Add Some Effects</h3> <p>Experiment with video effects by adding elements between <code class="language-plaintext highlighter-rouge">decodebin</code> and <code class="language-plaintext highlighter-rouge">autovideosink</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Edge detection</span>
gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>video.mp4 <span class="o">!</span> decodebin <span class="o">!</span> videoconvert <span class="o">!</span> edgetv <span class="o">!</span> autovideosink

<span class="c"># Color inversion</span>
gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>video.mp4 <span class="o">!</span> decodebin <span class="o">!</span> videoconvert <span class="o">!</span> videobalance <span class="nv">saturation</span><span class="o">=</span><span class="nt">-100</span> <span class="o">!</span> autovideosink

<span class="c"># Multiple effects</span>
gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>video.mp4 <span class="o">!</span> decodebin <span class="o">!</span> videoconvert <span class="o">!</span> warptv <span class="o">!</span> vertigotv <span class="o">!</span> autovideosink
</code></pre></div></div> <h3 id="3-modify-our-c-application">3. Modify Our C++ Application</h3> <p>Try these modifications to our basic video player:</p> <ul> <li>Add a volume control for audio playback</li> <li>Implement play/pause functionality</li> <li>Add a progress bar using video duration</li> <li>Handle window resize events</li> </ul> <p>Here’s a starting point for adding audio:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Add these elements</span>
<span class="n">GstElement</span><span class="o">*</span> <span class="n">audioconv</span> <span class="o">=</span> <span class="n">gst_element_factory_make</span><span class="p">(</span><span class="s">"audioconvert"</span><span class="p">,</span> <span class="s">"aconv"</span><span class="p">);</span>
<span class="n">GstElement</span><span class="o">*</span> <span class="n">audiosink</span> <span class="o">=</span> <span class="n">gst_element_factory_make</span><span class="p">(</span><span class="s">"autoaudiosink"</span><span class="p">,</span> <span class="s">"audio"</span><span class="p">);</span>

<span class="c1">// Don't forget to add them to the pipeline</span>
<span class="n">gst_bin_add_many</span><span class="p">(</span><span class="n">GST_BIN</span><span class="p">(</span><span class="n">pipeline</span><span class="p">),</span> <span class="n">audioconv</span><span class="p">,</span> <span class="n">audiosink</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">);</span>
</code></pre></div></div> <h3 id="4-explore-pipeline-states">4. Explore Pipeline States</h3> <p>Write a small program that demonstrates pipeline state changes:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Try changing states and observe behavior</span>
<span class="n">gst_element_set_state</span><span class="p">(</span><span class="n">pipeline</span><span class="p">,</span> <span class="n">GST_STATE_READY</span><span class="p">);</span>
<span class="n">gst_element_set_state</span><span class="p">(</span><span class="n">pipeline</span><span class="p">,</span> <span class="n">GST_STATE_PAUSED</span><span class="p">);</span>
<span class="n">gst_element_set_state</span><span class="p">(</span><span class="n">pipeline</span><span class="p">,</span> <span class="n">GST_STATE_PLAYING</span><span class="p">);</span>
</code></pre></div></div> <h3 id="5-build-a-multi-stream-pipeline">5. Build a Multi-Stream Pipeline</h3> <p>Try creating a pipeline that:</p> <ul> <li>Displays video in multiple windows</li> <li>Records video while displaying it</li> <li>Streams to network while playing locally</li> </ul> <p>Here’s an example:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Display and record simultaneously</span>
gst-launch-1.0 filesrc <span class="nv">location</span><span class="o">=</span>video.mp4 <span class="o">!</span> decodebin <span class="o">!</span> <span class="nb">tee </span><span class="nv">name</span><span class="o">=</span>t <span class="o">!</span> queue <span class="o">!</span> videoconvert <span class="o">!</span> autovideosink t. <span class="o">!</span> queue <span class="o">!</span> x264enc <span class="o">!</span> mp4mux <span class="o">!</span> filesink <span class="nv">location</span><span class="o">=</span>output.mp4
</code></pre></div></div> <h3 id="debug-tips">Debug Tips</h3> <p>When experimenting, these debug commands will be your friends:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># List all available elements</span>
gst-inspect-1.0

<span class="c"># Get detailed info about an element</span>
gst-inspect-1.0 videoconvert

<span class="c"># Enable debug output</span>
<span class="nb">export </span><span class="nv">GST_DEBUG</span><span class="o">=</span>3

<span class="c"># Debug specific elements</span>
<span class="nb">export </span><span class="nv">GST_DEBUG</span><span class="o">=</span><span class="s2">"videosink:6"</span>
</code></pre></div></div> <p>Remember, the best way to learn is by breaking things and fixing them!</p> <h2 id="conclusion">Conclusion</h2> <p>This guide has introduced you to the basics of GStreamer, from understanding its architecture to creating a simple pipeline and writing a basic video player in C++. GStreamer is a powerful tool for multimedia development, offering the flexibility to build both simple and complex media applications.</p> <p>In the upcoming articles, we’ll explore GStreamer plugins in greater detail, including how to develop custom plugins for your specific needs—whether you’re aiming to build a media player, a streaming server, or an AI video analytics platform.</p> <h3 id="next-steps">Next Steps</h3> <ul> <li><strong>Access the Code:</strong> All the code examples, detailed explanations, and installation instructions are available in the <a href="https://github.com/rosemary-crypto/build-with-gstreamer">GitHub repository</a>. Make sure to check it out to follow along with each article in the series.</li> <li><strong>Explore the Documentation:</strong> Dive into the GStreamer documentation to explore additional features and plugins.</li> <li><strong>Stay Tuned:</strong> Look forward to the next article, where we’ll discuss GStreamer plugins and how they work.</li> </ul>]]></content><author><name></name></author><category term="tutorials"/><category term="GStreamer,"/><category term="development,"/><category term="C++,"/><category term="AI,"/><category term="video"/><category term="analytics"/><summary type="html"><![CDATA[An introduction to GStreamer for building multimedia applications.]]></summary></entry></feed>